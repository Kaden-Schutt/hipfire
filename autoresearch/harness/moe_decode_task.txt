OPEN MoE-DECODE perf exploration. Maximize a3b QUANT decode tok/s on ARCH. Structured, harness-graded (NOT self-graded).
CONTEXT: work in WORKTREE. Baseline=BASELINE_REF. Model=MODELPATH. GPU dev=DEVN card=CARDN. First:
git checkout -B loop/cardCARDN BASELINE_REF. The branch tip is your current best; WIN commits stack on it.

TARGET the MoE decode kernels (biggest decode wall): gemv_hfq4g256_moe_gate_up_k8_indexed,
gemv_hfq4g256_moe_down_k8_indexed_batched_expanded, fused_qkvza_hfq4g256. SURFACE you may edit: their .hip
kernels AND their launch configs (block/grid/LDS) in crates/rdna-compute/src/{gemm,gemv}.rs + kernels.rs
_for_arch selectors. ALWAYS arch-gate launch-config edits to ARCH (if self.arch_caps.is_ARCHPRED(){<new>}else{<baseline>});
never edit the shared block in place. Fork gfx-specific kernels to <k>.ARCH.hip.

*** THE GRADER IS MECHANICAL + MANDATORY — you do NOT measure tok/s yourself. ***
For EACH candidate: (1) edit files (arch-gated), (2) generate patch: git add -A && git diff --cached BASELINE_REF -- kernels/src crates/rdna-compute/src > /tmp/candCARDN.patch && git reset -q && git checkout -q -- . (revert to a CLEAN tree), (3) GRADE:
   HIPFIRE_GPU_LOCKFILE=/tmp/gpu-cardCARDN.lock BASELINE_REF=BASELINE_REF MODEL=MODELPATH bash /tmp/ab_certify_v2b.sh ARCH DEVN CARDN MODELPATH <primary-kernel> <label> /tmp/candCARDN.patch
It prints ONE JSON verdict (adaptive-sampled, clock-gated, arch-bleed-guarded, coherence-checked). Act ONLY on it:
  WIN -> apply the patch + commit to loop/cardCARDN (git apply /tmp/candCARDN.patch; git add -A; git commit -m "WIN <label>").
  DEAD/NEUTRAL/VOID -> discard, try next. VOID -> re-grade (clock noise). ARCH_BLEED_FAIL -> you edited a shared launch
  constant; re-do arch-gated. PATCH_APPLY_FAIL/BUILD_FAIL -> fix the patch. Never trust your own timing.

EXPLORE FREELY: block/grid reshape, rows/block tiling, split-K with BARRIER-FREE reductions (S=4+__syncthreads was
DEAD -1.55%), multi-row-per-wave, dequant restructure, VOPD dual-issue, unroll tuning, LDS-stage x/scales, combos.
Already DEAD (skip): SLC/cache (MALL-resident), uint4 weight load (+VGPR), naive de-unroll (-ILP), global_load_lds
(ISA-unavailable on gfx11). Log each attempt + its v2b verdict so the search is legible. Budget your time; breadth first.
REPORT at end: cumulative decode tok/s (baseline->best) + each WIN + the arch-gated diff. Stay on loop/cardCARDN. No push/master.
