You are implementing a PRE-VETTED kernel-decode-optimization queue on the target arch (a3b AR decode). These levers were brainstormed + adversarially red-teamed across 3 workflow rounds. DO NOT re-brainstorm or invent new levers — your job is to IMPLEMENT the queued levers faithfully, run their pre-checks, and certify them. This is the anti-loser discipline: only queued, vetted levers get a GPU certify.

STEP 1 — READ THE QUEUE + PROGRESS:
  cat /tmp/certify_queue.json   (ordered levers; each has: order,name,kernel,arch,change,precheck,coherence_check,certify_protocol,realistic_delta,kill_condition,ready)
  cat /tmp/loop_progress.log    (already-attempted Q<order> items — NEVER redo one)
  Choose the next UNATTEMPTED item(s) whose arch matches THIS arch (or "both"), lowest `order` first. On a multi-card box pick up to (card count) DISTINCT items, one per card; on a single-card box pick exactly one.

STACKING DIRECTIVE (read each item's `certify_protocol`): several byte-EXACT items are BELOW the solo noise floor and say to certify "in the stack". For those disjoint byte-exact members (they hit different kernels), implement + token-id-parity-verify EACH, apply them CUMULATIVELY onto the baseline, and certify the COMBINED stack's decode tok/s once (the summed +4-6% clears the floor where each +0.4-1.6% would drown). Drop any member whose ISA precheck returns CUT. The pure-SHIP zero-precheck item (rmsnorm, order 1) anchors the stack. Non-byte-exact / sign-uncertain items certify individually.

STEP 2 — PRE-CHECK (skip only if the item's `precheck` is "none"):
  Apply the change to the target .hip, build, then run the .hsaco / gfx-kernel-metadata check named in `precheck` (VGPR count, s_waitcnt vmcnt depth, resident-wave count). If it shows the lever is a NO-OP (loads already hoisted) or a REGRESSION (occupancy drops a wave), LOG verdict=PRECHECK_FAIL and DO NOT run the GPU certify (that is a saved loser run). Otherwise continue.

STEP 3 — IMPLEMENT the item's `change` spec EXACTLY into its `kernel` .hip. Preserve exact output math as described in `coherence_check`. Write the variant to /tmp/loop_Q<order>.hip.

STEP 4 — CERTIFY the verbatim implementation (arch/dev/card/model/baseline are prefilled by the driver env; run as instructed for this box):
  ab_certify_v2.sh <arch> <dev> <card> <model> <kernel> Q<order>v0_<slug> /tmp/loop_Q<order>.hip
  ab_certify_v2 adaptively resamples (beats the DPM clock VOIDs), hard-gates coherence, commits WINs to the card branch. Trust ONLY its verdict.

STEP 4b — PERMUTATION SWEEP (the levers are MECHANISM-vetted, so a verbatim miss is a TUNING problem, not a dead lever — explore the parameter space before abandoning):
  - verbatim is a WIN  -> bank it, move on.
  - verbatim is DEAD-with-small-positive / INCONCLUSIVE / a near-miss / a COHERENCE_FAIL a reformulation could fix -> the mechanism is sound, so try UP TO 3 permutations varying the ONE key tunable of THIS lever, e.g.:
      * prefetch / pipeline / gather depth: 2-stage vs 3-stage vs 4-stage window
      * attention ring P: 2 vs 4 vs 8 (vs VGPR budget)
      * which loads get the treatment: weight-payload only vs +scale vs +zp; prefetch all 4 pk vs only 2 pk (to hold a tight VGPR/occupancy budget)
      * split-K factor; staging granularity; register-buffer vs RDNA3 global_load_lds
      * COHERENCE_FAIL -> a bit-parity-preserving reordering (fixed-order combine) so numerics stay identical
    Certify each (label Q<order>v1/v2/v3_<slug>); keep the best WIN. Mark the item EXHAUSTED only after verbatim + up to 3 permutations all fail.
  - EXCEPTIONS (do NOT sweep): a PRECHECK_FAIL from STEP 2 is dead at the ISA level; a LARGE hard-negative verbatim (mechanism clearly wrong for this arch) gets at most 1 permutation then stop. This keeps the sweep BOUNDED — a few tuned shots at a vetted lever, never blind generation.

STEP 5 — LOG one line per attempt (verbatim + each permutation) to /tmp/loop_progress.log:
  Q<order>v<n>_<slug> kernel=<kernel> lever=<name> perm=<what-varied> verdict=<WIN|DEAD|INCONCLUSIVE|COHERENCE_FAIL|VOID|PRECHECK_FAIL> delta_pct=<d> f=<f> rounds=<n> learned=<one line>

Do ONE round then stop: process ONE queue item per card (its verbatim certify + up to 3 permutation certifies). If every queue item for this arch is attempted+exhausted, output {"queue_drained":true}. Output compact JSON: {"round_done":true,"items":[{"q":..,"kernel":..,"best_verdict":..,"best_delta":..,"perms_tried":..}],"note":".."}
