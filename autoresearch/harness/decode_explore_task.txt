OPEN-ENDED PERF EXPLORATION — maximize a3b mq4r DECODE tok/s on gfx1100. Full decode hot path: the decode .hip
kernels AND their launch configs. Explore freely; keep what the GRADER says is faster. You do NOT grade yourself.

CONTEXT: ~/hipfire/.aw/sw_card0. Baseline = loop_baseline_gfx1100 (=baseline_v3, ~174-176 tok/s). Model
/home/kaden/.hipfire/models/qwen3.6-35b-a3b.mq4r, gfx1100 dev0. START: git checkout -B decode_explore
loop_baseline_gfx1100. The branch TIP is always your current best; each accepted win is a commit that stays.

*** THE GRADER IS MECHANICAL AND MANDATORY — you may NOT self-measure tok/s. ***
For EVERY candidate: make the change, `git add -A && git commit -m "try: <desc>"`, then run:
    bash /tmp/certify_v3.sh decode_explore
It prints ONE JSON verdict after enforcing (deterministically, you cannot influence it): ARCH-BLEED guard +
build + warmed clock-gated 4-run A/B vs baseline_v3 + coherence. Act ONLY on its verdict:
  - "WIN"            -> KEEP the commit (it stacks; the branch tip is your new best).
  - "DEAD"/"NEUTRAL" -> `git reset --hard HEAD~1` (drop it), try something else.
  - "VOID"           -> clock skew; re-run certify_v3 (don't change anything).
  - "COHERENCE_FAIL" -> your change garbles output; drop it.
  - "ARCH_BLEED_FAIL"-> YOU EDITED A SHARED LAUNCH CONSTANT. The detail lists the missing baseline block literal.
                        Re-do it ARCH-GATED: `if self.arch_caps.is_gfx1100(){<new>}else{<baseline literal, unchanged>}`.
                        Never edit the shared arm in place. (This is exactly what killed the split-K promotion.)
Do NOT trust your own timing/eyeballing — certify_v3's verdict is the only truth. Do NOT hand-roll a daemon A/B.

SURFACE YOU MAY EDIT: (1) kernels/src/<decode-kernel>.hip (qkvza, gate_up, moe_down, residual, others that fire
in a3b decode); fork to <k>.gfx1100.hip if gfx1100-specific. (2) their launch configs (block/grid/LDS) in
crates/rdna-compute/src/{gemm,gemv}.rs + the _for_arch selectors in kernels.rs — ALWAYS arch-gated to gfx1100.
FORBIDDEN: runtime/KV/sampler/daemon/forward-pass logic, and dispatch ROUTING (only the launch CONFIG of a kernel).

EXPLORE FREELY (no mandated lever): block/grid reshape, rows/block tiling, split-K with any S and BARRIER-FREE
partial reductions (the split-K DEAD was the __syncthreads barrier, not the idea), multi-row-per-wave, dequant
restructuring, VOPD dual-issue packing, unroll tuning, LDS-staging reused x/scales, fp16-WMMA reductions,
combinations. Already DEAD (don't repeat): plain SLC/cache (MALL-resident), uint4 weight load (+VGPR -2.5%),
naive de-unroll (-ILP), global_load_lds (ISA-unavailable gfx1100), S=4 split-K w/ __syncthreads (-1.55%).

Iterate broadly; log each attempt + its certify_v3 verdict so the search is legible. REPORT at end: cumulative
tok/s (baseline -> best), each WIN commit + its certify_v3 delta, and the final diff (all arch-gated). If nothing
clears WIN after a broad honest search, say so plainly with what you tried. Do NOT push/touch master. Stay on decode_explore.
