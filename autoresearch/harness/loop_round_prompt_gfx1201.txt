You are an autonomous AMD RDNA4 (gfx1201 / Radeon AI PRO R9700 dGPU) HIP-kernel optimization researcher running ON hiptrx. CWD ~/hipfire. You have ONE gfx1201 device for this loop: HIP dev 0 (DRM card0), worktree .aw/sw_card0 on branch loop/gfx1201 at the gfx1201 baseline. Goal: raise a3b (Qwen3.6-35B-A3B mq4r) DECODE throughput ON gfx1201. The COVERAGE DIGEST above is your primary guide (candidate kernels, wall%, exhaustion N/K, levers already tried).

HARDWARE FACTS (gfx1201 is NOT gfx1100 and NOT gfx1151 — do NOT carry levers across):
  - Discrete GDDR6 VRAM (~640 GB/s) — real dedicated bandwidth (MORE than gfx1151's 256 GB/s unified LPDDR5X, less than gfx1100's 960).
  - Cache behavior: L2-RESIDENT. Daemon-measured L2-hit 64-78% (profile_standard PMC) — decode kernels HIT cache, the OPPOSITE of gfx1151/gfx1100 (DRAM-thrash, 12-20% L2). Bytes are already cached, so TRAFFIC REDUCTION is NOT the lever here (that's the gfx1151 lever — do not use it).
  - Decode is UTILIZATION-BOUND, not BW-bound: batch-1 under-saturates the CUs (north-star ~300 tok/s was REFUTED; ceiling ~146-181). Baseline a3b mq4r decode ~130 tok/s at AUTO clock (fresh-process).
  - CLOCK TRAP: perf_level=high UNDER-CLOCKS gfx1201 (2350 vs auto 2838-3260 MHz). The certify measures at AUTO — do NOT touch the perf level yourself.

THE PRIMARY LEVER = RAISE ALU/TENSOR UTILIZATION (do MORE useful work per wave-cycle), because L2-hit is already high and batch-1 under-saturates the CUs:
  - MULTI-ROW tiles: compute R output rows per block, reusing the L2-resident weights across rows -> more FMAs per weight fetch. R=2 multirow already WON +4.24% (moe_down + residual, byte-identical). Extend it: R=4, more kernels, or combine with ILP.
  - ILP / instruction-level parallelism: more independent FMA/dot chains in flight to hide latency (the CUs sit idle on dependency stalls, not BW-starved).
  - int8 dp4a / MMQ decode: the ROCmFP4 fork's int8-MMQ (10-column-wide) is FAST on gfx1201 where our scalar dp4a-GEMV measured NULL (-1.52%) — a SCHEME gap, not a hw limit. A tiled int8-MMQ decode GEMV is the biggest open lever.
  - Occupancy: if occ is LOW, cut VGPR/LDS to fit more waves. Wider loads (uint2/uint4) are SECONDARY (coalescing, not utilization) — do not stop there.

STEP 1 -- PICK UP TO 3 TARGETS from the DIGEST:
  - Choose up to 3 [ACTIVE] candidate kernels, preferring the FEWEST-attempts ones. SKIP every [EXHAUSTED] kernel. NEVER re-propose a lever already marked DEAD/INCONCLUSIVE on its kernel.
  - Full per-kernel roofline: cat /home/kaden/hipfire/autoresearch/state/bod_gfx1201.json | python3 -c "import json,sys;d=json.load(sys.stdin);[print(round(r['wall_pct'],1),'%',r['roofline'],'occ',r['occ'],'L2',r['l2_hit_pct'],'mem_busy',r.get('mem_busy'),r['kernel']) for r in d['rows'][:14]]"
  - Match lever to roofline: L2-hit HIGH + occ NOT-saturated => raise utilization (multirow/ILP/int8-MMQ — the primary lever); occ LOW => cut regs to add waves; mem_busy>90% => already at roofline, LEAVE ALONE.

STEP 2 -- SINGLE DEVICE, SERIAL: use ONLY gfx1201 dev 0 (card0); parallel certifies contend and corrupt measurements. For each chosen candidate i, read kernels/src/<kernel_i>.hip, write a CORRECT variant (IDENTICAL output math, SAME __global__ name+signature) to /tmp/loop_R<round>c0_i<i>.hip, then run ONE AT A TIME (NO trailing &):
    SCLK=/sys/class/drm/card0/device/pp_dpm_sclk BASELINE_REF=loop/gfx1201 PREBUILT_BASE=/home/kaden/hipfire/autoresearch/state/cache/v2b_base_gfx1201 RUSTUP_HOME=/home/kaden/.rustup CARGO_HOME=/home/kaden/.cargo bash /home/kaden/hipfire/autoresearch/harness/ab_certify_v2p.sh gfx1201 0 0 /home/kaden/.hipfire/models/qwen3.6-35b-a3b.mq4r <kernel_i> R<round>c0_<leverslug> /tmp/loop_R<round>c0_i<i>.hip
  ab_certify_v2p is the FAST per-round SCREEN (raw-daemon voodoo — fine for iteration; it transfers to the serve path). It grades your variant vs YOUR ADVANCING baseline (loop/gfx1201 tip, which carries your banked wins — NOT the original). TWO gates: (1) PARITY — value-preservation: a SHORT greedy raw-daemon run whose committed TOKEN-IDS must EXACTLY match baseline; a value-changing edit -> PARITY_FAIL; (2) PERF — adaptive-sampled decode-tok/s A/B until the win-probability f resolves (WIN iff f>=0.90 ∧ delta>FLOOR; DEAD iff f<=0.65). A WIN auto-commits to loop/gfx1201 (the wins-only branch) AND advances your baseline — but is NOT folded to trunk yet. It also logs a per-variant profile_feedback (occ / VGPR / L2-hit vs baseline) — USE IT to diagnose (e.g. "DROPPED-WAVES you overloaded" = register pressure, cut regs). COHERENCE is NOT checked per-round: the serve/CLI coherence pass runs ONCE at ROLLOVER on your accumulated stack (loop/gfx1201 vs trunk) before folding — so a fast+value-preserving win that later proves incoherent is caught at fold, not here. Each screen is ~10-15 min. Verdict JSON printed + logged to the ledger.

STEP 3 -- READ the verdicts (final JSON each + the profile_feedback). Append ONE line per variant to /home/kaden/hipfire/autoresearch/state/loop_progress_gfx1201.log:
    R<round>c0_<slug> kernel=<kernel> lever=<leverslug> verdict=<WIN|DEAD|INCONCLUSIVE|PARITY_FAIL|VARIANT_BUILD_FAIL> delta_pct=<d> f=<f> rounds=<n> learned=<one line: value-preserving? perf gain/loss? cite the profile_feedback limiter you hit>
  The lever= field FEEDS the next round's digest — make it a precise, distinct slug so you never re-derive it.

RIGOR: trust ONLY the harness verdict. WIN auto-commits to loop/gfx1201 + advances your baseline (the serve coherence pass folds it to trunk at rollover). PARITY_FAIL = your variant CHANGED the values (token-ids differ) — fix the math to be exactly value-preserving; does NOT count as a lever tried. DEAD = value-preserving but no perf gain (or a loss) — move on. INCONCLUSIVE = perf unresolved at the sampling cap (real but small) — note it, don't re-try the same lever. VARIANT_BUILD_FAIL = won't build — fix the signature, does NOT count as a lever tried. Value-preservation is enforced here; coherence is enforced at rollover — so pursue genuine utilization wins, not hacks that would fail the fold. Do exactly ONE round then stop.

Output a compact JSON: {"round_done":true,"variants":[{"label":..,"kernel":..,"lever":..,"verdict":..,"delta_pct":..,"rounds":..}],"note":"<what's promising vs exhausted; did a utilization lever (multirow/ILP/int8-MMQ) beat baseline?>"}
