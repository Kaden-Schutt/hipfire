{'text': '{\n  "plan": "# Glimmer DFlash drafter — context-concatenated contract fix (plan)\\n\\n## 0. Denominators & non-negotiables (read first)\\n\\n- **Profile denominator:** current drafter is 100% wasted: `tau 1.016-1.022`, ~1/700 accepts, DFlash 1.8 tok/s vs AR 33 tok/s. Amdahl cap of the \\"fix K/V extent\\" bucket is 100% of DFlash time (the drafter does zero useful work). Any tau lift is meaningful; tau >=2 makes DFlash competitive.\\n- **Transfer loss is the prior:** current `broadcast(x)` + `L==B==16` code looks plausible (hidden l2 ≈ plausible) yet yields tau≈1.0. Compare against this tuned-but-wrong baseline, not upstream naive.\\n- **Gate-first:** the only unknown is whether window 2048 bidirectional vs full matters at ctx≈2k. Gate is a single fresh-process smoke on hardware (section 5) before repo-wide optimisations.\\n- **Bit-exactness / determinism:** reordering f32 accumulation (tiled softmax, j_chunk reduction) is bounded to ~1e-8 by existing proof. No atomics. RoPE order is per-row deterministic.\\n- **Arch-gate:** gfx1201 (Navi48) wave32, LDS 64KB. No gfx94x MFMA path touched.\\n- **Control plane is Rust-only.** No JS/TS surface.\\n- **Validation authority:** `docs/VALIDATION.md` (§Retired coherence-gate) — `coherence-gate-dflash.sh` is retired. No universal gate. Claim class == forward numerical parity → path-specific smoke + tau/tok_s measurement (section 5). `cargo check --workspace --all-targets` is the automatic gate.\\n- **Constraints from ticket:** NO SILENT STUBS, no new kernels, `git diff beta` deletions only in `crates/hipfire-arch-muse-glimmer/` + additive daemon hooks, `cargo check --workspace --all-targets` clean, do not commit/fmt.\\n\\n### Reference anchors (frozen)\\n\\n- Upstream: `\\\\`/tmp/ga_modeling.py:296-312\\\\`` (`MuseGlimmerAssistantContextProjection.fc + output_norm_enc`), `\\\\`/tmp/ga_modeling.py:138-196\\\\`` (Attention: `kv_hidden_states = cat([context_hidden_states, hidden_states])`, `q_proj(hidden_states)` -> B, `k/v_proj(kv_hidden_states)` -> ctx+B, `q_norm/k_norm` weighted, `apply_rotary_pos_emb(q,k,cos,sin)` over ctx+B), `\\\\`/tmp/ga_modeling.py:371-444\\\\`` (Model: `context_hidden_states=self.encoder(context_hidden_states)` once, `position_ids=arange(noise_embeds.shape[1]+context_hidden_states.shape[1])+past_seen`, `hidden_states=noise_embeds` NO add, loop `layer(hidden, context_hidden)`).\\n- Local distilled contract: `\\\\`~/.omp/wt/glimmer/local-glimmer-drafter-contract.md:19-65\\\\``\\n- Current divergence table: `local-glimmer-drafter-contract.md:66-83`\\n\\n---\\n\\n## 1. Frozen cross-slice contracts\\n\\n### 1.1 Scratch contract — `crates/hipfire-arch-muse-glimmer/src/drafter.rs:403-475`\\n\\n```rust\\npub struct GlimmerDrafterScratch {\\n    pub x: GpuTensor,                   // [block*hidden]      block=16, hidden=6656\\n    pub target_hidden_proj: GpuTensor,  // [max_ctx*hidden]    max_ctx = ctx_cap (see 1.3)\\n    pub q: GpuTensor,                   // [block*q_dim]       q_dim=4096  (32*128)  Q-only\\n    pub k: GpuTensor,                   // [(max_ctx+block)*kv_dim]  kv_dim=1024 (8*128)  ← restored\\n    pub v: GpuTensor,                   // [(max_ctx+block)*kv_dim]           ← restored\\n    pub attn_out: GpuTensor,            // [block*q_dim]\\n    pub tmp: GpuTensor,                 // [block*hidden]\\n    pub gate_ffn: GpuTensor,            // [block*intermediate]  19968\\n    pub up_ffn: GpuTensor,\\n    pub ffn_hidden: GpuTensor,\\n    pub logits_tmp: GpuTensor,          // [hidden]\\n    pub pos_buf: hip_bridge::DeviceBuffer, // (max_ctx+block)*4 bytes  ← was 4 bytes\\n}\\n// ctor: fn new(gpu:&mut Gpu, cfg:&GlimmerDrafterConfig, max_ctx:usize) -> Result<Self,String>\\n// - max_ctx passed from loader is min(max_seq, HIPFIRE_DFLASH_CTX_CAP|8192) capped to sliding_window (see 1.3)\\n// - alloc: x=block*h, target_hidden_proj=max_ctx*h, q=block*qd, k/v=(max_ctx+block)*kvd, pos_buf=(max_ctx+block)*4\\n// - free_gpu must free all 10 tensors + pos_buf\\n```\\n\\nExisting bug: `drafter.rs:428-435` mallocs 4 bytes; `drafter.rs:408-410` sizes k/v as `block*kvd`. History had `(max_ctx+block)*kvd` and was reduced — restore.\\n\\n### 1.2 Forward contract — `crates/hipfire-arch-muse-glimmer/src/drafter.rs:476-613`\\n\\n```rust\\n// BEFORE (current):\\n//   fn glimmer_drafter_forward(gpu,cfg,weights,scratch,\\n//       noise_embedding:&[f32], target_hidden:&[f32],\\n//       positions_q:&[i32], positions_k:&[i32], block_size, ctx_len)\\n//   where positions_q/k.len()==block, attention L==B, broadcast add, per-row rope on paired Q/K\\n\\n// AFTER (frozen):\\npub fn glimmer_drafter_forward(\\n    gpu: &mut Gpu,\\n    cfg: &GlimmerDrafterConfig,\\n    weights: &GlimmerDrafterWeights,\\n    scratch: &mut GlimmerDrafterScratch,\\n    noise_embedding: &[f32],                 // [block*hidden] raw, no embed_norm\\n    target_hidden: &[f32],                  // [ctx_len * num_extract*hidden]  num_extract=5, 33280/row\\n    positions: &[i32],                      // [ctx_len+block_size] single span, tail is Q positions\\n    block_size: usize,                      // == cfg.block_size ==16\\n    ctx_len: usize,                         // 0..=max_ctx, after capping (see 4)\\n) -> Result<(), String>\\n// Invariants:\\n// - noise_embedding.len()==block*hidden else Err\\n// - target_hidden.len()==ctx_len*5*hidden else Err\\n// - positions.len()==ctx_len+block_size else Err(\\"positions size mismatch\\")\\n// - positions == [cur_pos-ctx_len .. cur_pos+block-1] absolute (continuous). Q positions = positions[ctx_len..]\\n// - cfg.mask_token_id==201818 else Err\\n```\\n\\nSemantics (must match §Reference anchors, no approximation):\\n1. `target_hidden_proj = rmsnorm(fc * target_hidden)` per ctx row ONCE, via `weight_gemv` + `rmsnorm_f32` looping `0..ctx_len` using `scratch.tmp` as staging. `fc` is `WeightTensor [hidden, 5*hidden]` (`encoder.fc.weight`), `output_norm_enc` is `GpuTensor [hidden]`. Reused by all 5 layers (not recomputed per layer).\\n2. `scratch.x = noise_embedding` via `memcpy_htod` ONLY — **remove** `drafter.rs:539-546` broadcast `add_inplace_f32(last_proj)` loop entirely.\\n3. Per-layer (5×):\\n   - `n1 = rmsnorm_batched(x, input_layernorm, tmp, B, h, eps)`  `eps=1e-5`\\n   - `q = q_proj(n1)` per block row (B rows, B*qd)\\n   - `kv_hidden = cat(ctx_proj, n1)` conceptually → `k = k_proj(kv_hidden)` over ctx+B rows, `v = v_proj(kv_hidden)` over ctx+B rows (loop `0..ctx_len` from `target_hidden_proj` slices, `0..B` from `n1` slices into `k`/`v` tail).\\n   - `rmsnorm_batched(q, q_norm, q, B*n_heads, head_dim, eps)` and `rmsnorm_batched(k, k_norm, k, (ctx+B)*n_kv_heads, head_dim, eps)` — weighted per-head (real q/k_norm weights, not scale-less).\\n   - RoPE half-split (`rope_theta=500000`) over concatenated extent: `Q` rows use `positions[ctx_len..]` (B entries), `K` rows use `positions[..]` (ctx+B entries). Implementation may use `rope_f32` with `n_heads_q=0` for ctx-K-only rows and `n_heads_k=0` for Q-only (kernel `kernels/src/rope.hip:7-40` skips loop when count 0, pointer dummy valid). Batch fast-path is `rope_batched_f32` where noted; no new kernel.\\n   - `attention_dflash_f32(&q, &k, &v, &attn_out, B, L=ctx+B, 32, 8, 128)` — see 1.4 for mask.\\n   - remaining block unchanged: `o_proj` per row → `tmp`, `x += tmp`, `rmsnorm_batched(x, post_attention_layernorm, tmp, B, ...)`, `gate/up/down` + `silu_mul_f32` + residual, `x=rmsnorm_batched(x, norm, x, B, ...)` final.\\n4. No other signature change; return `Ok(())` with `scratch.x` holding final normed hidden (caller runs `lm_head` over rows `1..B-1`).\\n\\n### 1.3 Daemon ↔ loader contract\\n\\n- `MuseGlimmerBundle.target_hidden_host: Vec<f32>` in `crates/hipfire-loader/src/lib.rs:373` is `[n_committed * 33280]` host concat in order `[1,13,25,37,49]`. Grown in `daemon.rs:29242` (prefill) and `daemon.rs:29532` (verify), truncated to `hidden_before+keep_rows*33280` at `daemon.rs:29542`.\\n- Loader `carriers.rs:1864` already sizes `GlimmerDrafterScratch::new(ctx.gpu, &dcfg, ctx.max_seq)` with `ctx.max_seq = min(requested_max_seq, 512K)` capped via `HIPFIRE_DFLASH_CTX_CAP|8192` at `daemon.rs:19024`. New bound: `max_ctx = min(ctx.max_seq, cfg.sliding_window)` with `sliding_window=2048` (i.e. 2048). Scratch therefore needs `(2048+16)*1024 ≈2.1M f32` per K/V ≈8 MB each — trivial.\\n- Daemon call site frozen:\\n```rust\\n// file: crates/hipfire-runtime/examples/daemon.rs:29346-29416\\nlet row_elems = ne * hidden; // 33280\\nlet n_rows = bundle.target_hidden_host.len() / row_elems;\\nlet ctx_cap: usize = drafter.config.sliding_window; // 2048 — or sliding_window.saturating_sub(block_size) =2032 (choose one and document)\\nlet ctx_len = n_rows.min(ctx_cap);\\nlet start_elem = (n_rows - ctx_len) * row_elems; // suffix (most recent window)\\nlet target_hidden: Vec<f32> = bundle.target_hidden_host[start_elem..].to_vec(); // [] when n_rows==0\\nlet cur_pos = bundle.state.n_tokens as i32; // absolute position of seed (= next write slot)\\nlet positions: Vec<i32> = (cur_pos - ctx_len as i32 .. cur_pos + block_size as i32).collect(); // len ctx+B, tail is Q\\nlet ok = glimmer::drafter::glimmer_drafter_forward(gpu, &drafter.config, &drafter.weights, &mut drafter.scratch,\\n    &noise_embedding, &target_hidden, &positions, block_size, ctx_len);\\n```\\n- Env: `HIPFIRE_DFLASH_DRAFT` selects draft .hfq, `HIPFIRE_GLIMMER_SPEC_DIAG=1` enables l2 diag, `HIPFIRE_GLIMMER_TAP_LAYERS` selects tap layers — unchanged. `HIPFIRE_DFLASH_CTX_CAP` may further cap `max_ctx`.\\n\\nChosen bound to report: **`ctx_len = min(n_accepted, sliding_window)` with suffix truncation (keep most recent 2048 rows), i.e. 2048**. Alternative conservative `2032 = 2048-block` is also correct (ensures `ctx+B ≤2048` so windowed==full). Either is acceptable if documented; prefer **2048** (full window, drop oldest) because upstream window is 2048 and `ctx+B=2064` overflow is 16 tokens — negligible. **State the bound explicitly in the tape log** (`eprintln!(\\"[glimmer-spec] ctx {}\\\\/{}\\", ctx_len, ctx_cap)`).\\n\\n### 1.4 Attention mask contract\\n\\n- `attention_dflash_f32` at `crates/rdna-compute/src/attention.rs:6778` + `kernels/src/attention_dflash.hip:33-40` explicitly supports `B != L` (`q: [B*nh*hd], k/v: [L*nkv*hd], out: [B*nh*hd], Grid [n_heads, B]`) with tiled online-softmax, non-causal (bidirectional) — exactly the needed \\"Q=B against K/V=ctx+B bidirectional\\" contract. No causal mask, GQA 32/8, hd 128, f32.\\n- Sliding window 2048: `attention_dflash_f32` is **full** bidirectional, not windowed. No existing helper expresses `B` vs `ctx+B` **and** a 2048 sliding window bidirectionally without a new kernel. Nearest windowed helpers are `attention_q8_0_kv_swa`/`attention_q8_0_flash_prefill_wmma` but they require Q8 quantized KV and/or causal decode shape — rejected for this DFlash f32 path (see `drafter.rs:42-57` rationale). Therefore:\\n  - For `ctx+B ≤2048` (or `≤2032` if bounding there) the full kernel is **exact** (window == full).\\n  - For larger `ctx` the full kernel over-attends to distant context by at most `ctx+B-2048 ≤~16` tokens if capping at 2048. This is the documented approximation. **Do not silently fall back to `L==B`; do not add a new kernel; do state the approximation in code comment.**\\n  - If no helper could express `B != L`, the plan would STOP — not applicable here because `attention_dflash_f32` does.\\n\\n---\\n\\n## 2. Slice decomposition (composer-owned, parallelizable)\\n\\n### Slice A — `GlimmerDrafterScratch + glimmer_drafter_forward` (single file, one owner)\\n\\n**Owner:** backend-kernel (hipfire-arch-muse-glimmer)\\n**Target:** `crates/hipfire-arch-muse-glimmer/src/drafter.rs`\\n**Non-goals:** daemon, loader, any other crate.\\n\\n**Change — step by step:**\\n- `drafter.rs:1-68` header comment: replace broadcast/L==B paragraph (`Extent decision` at `drafter.rs:33-40`) with concatenated contract: ctx_proj once, K/V=ctx+B, Q=B, positions span ctx+B, attention_dflash_f32 B vs L, window approximation note.\\n- `drafter.rs:405-425` struct: `q: [B*qd]` stays, `k/v: [(max_ctx+block)*kvd]` (restore), `pos_buf: DeviceBuffer` comment update to `(max_ctx+block)*4`.\\n- `drafter.rs:427-455` `Scratch::new`: `max_ctx` param already exists via `carriers.rs:1864` (`ctx.max_seq`). Compute `qd/kvd/block/h`, `alloc(\\"q\\", block*qd)`, `alloc(\\"k\\", (max_ctx+block)*kvd)`, `alloc(\\"v\\", (max_ctx+block)*kvd)`, `pos_buf = hip.malloc((max_ctx+block)*4)`, plus existing `target_hidden_proj = max_ctx*h`, etc. Keep `free_gpu` matching.\\n- `drafter.rs:484-613` `glimmer_drafter_forward`:\\n  - Signature: replace `positions_q:&[i32], positions_k:&[i32]` with `positions:&[i32]` (single span) as frozen in 1.2; update doc comment `noise_embedding`/`target_hidden`/`positions` accordingly. Keep `block_size, ctx_len` trailing args.\\n  - Validate `positions.len()==ctx_len+block_size` (error string must mention expected vs got), `target_hidden.len()==ctx_len*5*hidden`, `noise_embedding.len()==block*hidden`, `mask_token_id==201818`.\\n  - Keep `drafter.rs:521-533` per-row `fc` loop over `0..ctx_len` (host upload into `tmp.sub_offset(0, ne*h)` → `weight_gemv(fc, tmp_in, target_hidden_proj.row)` → `rmsnorm_f32(output_norm_enc)`) — this already is the real GPU path over n_ctx rows (not the described host shortcut). Ensure it remains and is not replaced by a host matmul. If any `ctx_len==1` fast-path exists, delete it.\\n  - Delete `drafter.rs:539-546` broadcast add entirely (the `last_proj` loop). `x` is noise alone.\\n  - Per-layer loop `drafter.rs:548-609`:\\n    * `rmsnorm_batched(x, input_layernorm → tmp, B)` unchanged.\\n    * Projection: `q` over B rows from `tmp`; `k/v` over ctx+B rows: first `ctx_len` rows from `target_hidden_proj` slices, remaining `B` rows from `tmp` slices. Use two sub-loops per layer for `k_proj`/`v_proj` (ctx then block tail). Sizes now match restored scratch.\\n    * Norms: `rmsnorm_batched(q, q_norm, q, B*nh, hd)`, `rmsnorm_batched(k, k_norm, k, (ctx+B)*nkv, hd)` — note k batch is ctx+B, not B.\\n    * RoPE: implement as described in 1.2. Host builds `positions` already (daemon), but inside forward we must upload to `pos_buf` and call roping. For tail: loop `i in 0..B` with `pos=positions[ctx_len+i]`, `q_row = q.sub_offset(i*qd,qd)`, `k_tail_row = k.sub_offset((ctx_len+i)*kvd,kvd)` → `rope_f32(&q_row, &k_tail_row, &pos_buf_single, n_heads, n_kv_heads, hd, theta)` where `pos_buf_single` is a 1-int DeviceBuffer slice (`pos_buf` offset `i*4`? existing impl uses 1-int `pos_buf` per call via `memcpy_htod`). For ctx head: either per-row `rope_f32` with `n_heads_q=0` (q dummy) or one `rope_batched_f32` for ctx batch (preferred if feasible without new alloc). Document choice and note that per-row ctx loop is ≤2048×5 launches — acceptable for 48-token smoke, batch optimization is follow-up.\\n    * Attention: `gpu.attention_dflash_f32(&q, &k, &v, &attn_out, B, ctx_len+B, 32,8,128)` — note `L = ctx+B`, not `B`.\\n    * Rest (o_proj, residuals, FFN, final norm) unchanged except they read `tmp`/`x` which now hold correct attention output.\\n  - Update comment at `drafter.rs:573-585` to reflect `L = ctx+B` and window note.\\n- Add `#[cfg(test)]` unchanged (`qkv_dims_match_config` at `drafter.rs:615-640`), optionally add `positions_len_mismatch` test for new invariant.\\n\\n**Acceptance (slice A):**\\n- `cargo check --workspace --all-targets` clean.\\n- `git diff beta --stat` shows changes only inside `crates/hipfire-arch-muse-glimmer/` for this slice.\\n- Scratch `k`/`v` capacity equals `(max_ctx+block)*kvd`; `pos_buf` capacity `(max_ctx+block)*4`; `q` stays `block*qd`.\\n- `glimmer_drafter_forward` no longer contains `add_inplace_f32(&dst,&last_proj)` broadcast; `attention_dflash_f32` call uses `l = ctx_len+block` and `k`/`v` are `ctx+B` sized.\\n- Unit test `positions size mismatch` trips when `positions.len()!=ctx+B`.\\n\\n**Measure gate:** local `cargo check` only; perf is slice B\'s gate.\\n**Abandon:** if `k/v` allocation for `max_ctx=8192` OOMs on 9070 (unlikely: 8192*1024≈8M f32≈33 MB per buffer), cap `max_ctx` to `min(max_ctx, sliding_window)` and retry — not abandon.\\n\\n### Slice B — Daemon `generate_muse_glimmer` context + positions (single file, one owner)\\n\\n**Owner:** runtime-daemon\\n**Target:** `crates/hipfire-runtime/examples/daemon.rs` — inside `fn generate_muse_glimmer` (`daemon.rs:29030` via search `glimmer_drafter_forward`)\\n**Non-goals:** drafter crate, loader, other generate paths (gemma4/lfm2moe remain AR).\\n\\n**Change — step by step:**\\n- Locate block at `daemon.rs:29392-29416` (comment `Target hidden history: last ctx row`, `let mut target_hidden = if ... last row ..`, `let ctx_len: usize =1`, `let positions_q/k: Vec<i32> = (cur_pos .. cur_pos+block)`, `glimmer_drafter_forward(..., &positions_q, &positions_k, block, ctx_len)`).\\n- Replace with:\\n```rust\\nlet ne = drafter.config.num_extract(); //5\\nlet hidden = drafter.config.hidden; //6656\\nlet row_elems = ne*hidden; //33280\\nlet n_rows = bundle.target_hidden_host.len() / row_elems;\\nlet ctx_cap = drafter.config.sliding_window; // 2048 — document bound; alternative 2032=2048-block\\nlet ctx_len = n_rows.min(ctx_cap);\\nlet target_hidden: Vec<f32> = if ctx_len==0 { Vec::new() } else {\\n    let start = (n_rows - ctx_len)*row_elems;\\n    bundle.target_hidden_host[start..].to_vec() // suffix, keep most recent\\n};\\nlet positions: Vec<i32> = (cur_pos as i32 - ctx_len as i32 .. cur_pos as i32 + block_size as i32).collect();\\n// assert positions.len()==ctx_len+block_size\\nlet ok = glimmer::drafter::glimmer_drafter_forward(gpu, &drafter.config, &drafter.weights, &mut drafter.scratch,\\n    &noise_embedding, &target_hidden, &positions, block_size, ctx_len);\\n```\\n  Remove `positions_q/k` block-sized vectors and `ctx_len=1` hardcode.\\n- Update diag at `daemon.rs:29448-29479` (`HIPFIRE_GLIMMER_SPEC_DIAG`) to report `ctx_len` and `positions` head/tail: `eprintln!(\\"[glimmer-spec] ctx {}/{} cur_pos {} pos head {:?} tail {:?}\\", ctx_len, ctx_cap, cur_pos, &positions[..2.min], &positions[positions.len()-2..])` and existing `target_hidden` l2 already logs — add `target_hidden` rows count.\\n- Keep `total_proposed/accepted/windows` tau logging at `daemon.rs:29545-29549` unchanged (now denominator grows because drafts are conditioned, not zero).\\n- No other daemon paths (AR fallback `daemon.rs:29629`, verify `daemon.rs:29532` with `verify_block_with_capture` and truncate) — keep as is; truncation already preserves `target_hidden_host` suffix invariant.\\n- `git diff beta` for this slice is additive daemon hooks only (no deletions outside glimmer crate).\\n\\n**Acceptance (slice B):**\\n- Same `cargo check` clean.\\n- On a 48-token smoke (`/tmp/glimmer-smoke.py x 48` via `ssh hiptrx … HIP_VISIBLE_DEVICES=3 … timeout 500 … grep -E \\"done:|drafts sample\\" /tmp/gdiag3.log`), `ctx_len` grows across windows (not stuck at 1), `positions.len()==ctx_len+16`, `target_hidden.len()==ctx_len*33280`.\\n- No `ctx_len==1` hardcode remains (`grep -n \\"ctx_len.*1\\" daemon.rs` shows only the bound).\\n\\n**Measure gate:** hardware smoke — see §5.\\n**Abandon:** if `bundle.target_hidden_host` is observed empty after prefill (l2 0), the capture path `decode_step_with_capture` at `daemon.rs:29230-242` is broken — fix capture before claiming drafter fix; not a drafter logic abandon.\\n\\n---\\n\\n## 3. Call sites, flags, env, and build order\\n\\n**Call sites:**\\n- `drafter.rs:485` `glimmer_drafter_forward` — sole caller is `daemon.rs:29412` (`glimmer_drafter_forward` search). Also potential test harness; update all.\\n- `GlimmerDrafterScratch::new` — sole caller `carriers.rs:1864` (`ctx.max_seq`), already passes `max_ctx` correctly.\\n\\n**Flags/env (no new env):**\\n- `HIPFIRE_DFLASH_DRAFT=/home/kaden/.hipfire/models/muse-glimmer-30b-assistant.q8.hfq` (draft selector)\\n- `HIPFIRE_GLIMMER_SPEC_DIAG=1` (per-stage l2)\\n- `HIPFIRE_GLIMMER_TAP_LAYERS=0,12,24,36,48` etc (tap A/B, not changed)\\n- `HIPFIRE_GLIMMER_SPEC_PERTURB=1` (perturb check), `HIPFIRE_GLIMMER_SPEC_FALLBACK=1` (fallback to AR)\\n- `HIPFIRE_DFLASH_CTX_CAP|HIPFIRE_JINJA_CHAT` — capped max_ctx\\n\\n**Ordered build sequence (do not reorder):**\\n1. Apply Slice A (`drafter.rs`) first — it changes the API surface. `cargo check --workspace --all-targets` must pass with Slice A alone (daemon will not compile until Slice B updates the call site — so check Slice A with `cargo check -p hipfire-arch-muse-glimmer` only).\\n2. Apply Slice B (`daemon.rs`) to match new `positions` signature. Then `cargo check --workspace --all-targets` clean end-to-end.\\n3. `cargo build --release --features deltanet --example daemon -p hipfire-runtime` on hiptrx (worktree `\\\\`/home/kaden/wt-glimmer\\\\``, branch `muse-glimmer`, device 3). Use `scp` of changed files before build (edits are local, parent commits).\\n4. Smoke run (see §5) — do not run project-wide test suites/formatters (caller does shared validation).\\n\\n---\\n\\n## 4. What we are NOT doing (explicit)\\n\\n- No new HIP kernel. RoPE batching reuses `rope_f32`/`rope_batched_f32` (`rdna-compute/src/norm.rs:763`, `823`) and `attention_dflash_f32` (`attention.rs:6778`).\\n- No change to `target_hidden` capture (`forward.rs:468-513` `decode_step_with_capture`, `verify_block_with_capture:519`) — already verified healthy (`l2 665.29`, 33280 non-zero) and truncation at `daemon.rs:29542`.\\n- No tap-layer re-indexing — A/B on hardware `[1,13,25,37,49]` vs `[0,12,24,36,48]` already identical tau, not bottleneck.\\n- No change to `q_norm/k_norm` weighting, `norm_eps 1e-5`, `rope_theta 500000`, `mask_token_id 201818` — already fixed.\\n- No shared-code deletions outside `crates/hipfire-arch-muse-glimmer/`.\\n- No safetensors/Dir load for glimmer (`carriers.rs:1909` remains unwired).\\n- No Redline/PM4/AQL tape work.\\n\\n---\\n\\n## 5. Measurement gates & abandon criteria (claim-scoped, VALIDATION.md §Claim→route)\\n\\n**Automatic gate:** `cargo check --workspace --all-targets` clean.\\n\\n**Manual GPU gates (hiptrx, device 3 only, 0/1/2 in use):**\\n\\nBuild:\\n```\\ncd /home/kaden/wt-glimmer && git fetch -q origin muse-glimmer && git checkout -q -f FETCH_HEAD\\n# scp local drafter.rs + daemon.rs into worktree, then\\ncargo build --release --features deltanet --example daemon -p hipfire-runtime\\n```\\nRun (single fresh-process, then triplicate if promising):\\n```\\nssh hiptrx \'cd /tmp && HOME=/home/kaden/glimmer-home-3 GLIMMER_DAEMON=/home/kaden/wt-glimmer/target/release/examples/daemon GLIMMER_PROMPT_FILE=/tmp/glimmer_prompt.txt GLIMMER_ERRLOG=/tmp/gdiag3.log HIP_VISIBLE_DEVICES=3 HIPFIRE_DFLASH_DRAFT=/home/kaden/.hipfire/models/muse-glimmer-30b-assistant.q8.hfq timeout 500 python3 /tmp/glimmer-smoke.py x 48 >/dev/null 2>&1; grep -E \\"done:|drafts sample|glimmer-spec.*tau|glimmer-diag|ctx\\" /tmp/gdiag3.log | tail -30\'\\n```\\nWith `HIPFIRE_GLIMMER_SPEC_DIAG=1` for first 2 windows (l2 diag), without for perf.\\n\\n**Metrics to report (≥3 fresh-process runs, record prompt md5 + binary md5, eyeball decoded output):**\\n- `tau = (total_accepted + windows)/windows` (bonus counted, same as `daemon.rs:29626`). Baseline to beat: **1.016-1.022, ~1/700 accepts**.\\n- `tok/s` AR vs DFlash from same prompt (AR is `use_spec=false` path at `daemon.rs:29629`; DFlash is `use_spec=true` at `daemon.rs:29288`). Report both.\\n- Byte-identical at temp 0: `HIPFIRE_GLIMMER_SPEC_PERTURB=1` must show `PERTURB CHECK OK` (forcing `draft[0]=pick[0]` changes accept) — proves comparison sensitive and drafter ran. Greedy argmax divergence without perturb is a correctness bug, not tau.\\n- Sanity: `target_hidden l2` ~665, `noise_embedding` non-zero, `draft hidden row` l2 finite, `drafts sample` varies per window (not constant 0).\\n\\n**Success:** any statistically stable tau lift over 1.022 is meaningful; tau ≥2 is competitive. Even modest 1.2-1.5 with stable tok/s lift is a valid improvement to ship default-OFF behind env if not bit-exact? But this change is bit-exact by construction (no reordering beyond tiled attention\'s 1e-8), so default ON is allowed once tau lifted.\\n\\n**Honest negative (valid result):** if after correct contract (verified by code review: no broadcast, K/V=ctx+B, positions span ctx+B, ctx_len grows, attention L=ctx+B) tau remains ~1.02 and `drafts sample` varies, report plainly with evidence (3 runs, prompt md5, binary md5, tau, tok/s, decoded output eyed). Best evidence-backed reading of why (e.g., tap-layer shift already A/B\'d identical; remaining suspects: target_hidden capture order, qk_norm weight convention, or training-vs-inference mismatch — but do not re-investigate beyond stating).\\n\\n**Abandon / STOP:**\\n- If no existing attention helper can express `Q=B` vs `K/V=ctx+B` **and** bidirectional mask without a new kernel, STOP and report rather than silently falling back to `L==B`. Not triggered — `attention_dflash_f32` at `attention.rs:6778` + `kernels/src/attention_dflash.hip:10-17` does express it (tiled, GQA, f32, non-causal). Sliding window 2048 is the only unexpressed nuance — document as full-attention approximation (exact when `ctx+B ≤2048`).\\n- If per-row RoPE for ctx (≤2048 rows) causes >50% wall-time regression on 48-token smoke (profile `attention_dflash_f32` vs RoPE time), batch RoPE into `rope_batched_f32` before claiming perf — not abandon, just optimize.\\n\\n---\\n\\n## 6. Risks & mitigations\\n\\n- **Pos_buf lifetime** (`drafter.rs:424` was malloc 4 then freed per-layer, fixed to persistent) — keep persistent, free in `free_gpu` only.\\n- **Weight_gemv staging** (`drafter.rs:524-531` uses `tmp.sub_offset`) — ensure `tmp` is `block*hidden` (≥33280) so `ne*h` fits for fc staging (33280). It does.\\n- **Silent truncation:** daemon must suffix-truncate oldest ctx rows when `n_rows > ctx_cap`, not prefix — otherwise recent context lost. Log `ctx_len` each window.\\n- **Determinism:** tiled `attention_dflash_f32` multi-tile carries `(max,sum,out)`; single-tile vs multi-tile differ by FP order <1e-7 — acceptable for greedy argmax.\\n\\n---\\n\\n```mermaid\\nflowchart LR\\n  subgraph daemon[\\"daemon.rs generate_muse_glimmer\\"]\\n    TH[\\"target_hidden_host [n*33280]\\"] -->|suffix ctx_cap=2048| TH2[\\"target_hidden [ctx*33280]\\"]\\n    NP[\\"noise_embedding [16*6656] raw\\"] --> D\\n    POS[\\"positions [ctx+16]\\"] --> D\\n  end\\n  subgraph drafter[\\"drafter.rs glimmer_drafter_forward\\"]\\n    D -->|fc + output_norm_enc ONCE| CTP[\\"ctx_proj [ctx*6656]\\"]\\n    CTP -->|cat per layer| K[\\"k/v [ctx+16 x kvd]\\"]\\n    NP -->|input_layernorm| Q[\\"q [16 x qd]\\"]\\n    Q & K -->|rope over ctx+16| R\\n    R -->|attention_dflash_f32 B=16 L=ctx+16 bidir| A[\\"attn_out\\"]\\n    A --> FFN --> X[\\"x [16*6656] final norm\\"]\\n  end\\n  X -->|lm_head slice 1..| LOGITS[\\"drafts [15]\\"]\\n  LOGITS --> VERIFY[\\"verify_block_with_capture\\"]\\n```\\n"\n}', 'details': {'resolvedPath': '/home/kaden/.omp/agent/sessions/-ClaudeCode-autorocm-hipfire-a3b-lowbit/2026-08-10T17-26-35-871Z_019fecb6-445f-7000-848f-7db1adfe4586/GlimmerDraftCtx.md', 'contentType': 'text/markdown', 'totalLines': 3, 'displayContent': {'text': '{\n  "plan": "# Glimmer DFlash drafter — context-concatenated contract fix (plan)\\n\\n## 0. Denominators & non-negotiables (read first)\\n\\n- **Profile denominator:** current drafter is 100% wasted: `tau 1.016-1.022`, ~1/700 accepts, DFlash 1.8 tok/s vs AR 33 tok/s. Amdahl cap of the \\"fix K/V extent\\" bucket is 100% of DFlash time (the drafter does zero useful work). Any tau lift is meaningful; tau >=2 makes DFlash competitive.\\n- **Transfer loss is the prior:** current `broadcast(x)` + `L==B==16` code looks plausible (hidden l2 ≈ plausible) yet yields tau≈1.0. Compare against this tuned-but-wrong baseline, not upstream naive.\\n- **Gate-first:** the only unknown is whether window 2048 bidirectional vs full matters at ctx≈2k. Gate is a single fresh-process smoke on hardware (section 5) before repo-wide optimisations.\\n- **Bit-exactness / determinism:** reordering f32 accumulation (tiled softmax, j_chunk reduction) is bounded to ~1e-8 by existing proof. No atomics. RoPE order is per-row deterministic.\\n- **Arch-gate:** gfx1201 (Navi48) wave32, LDS 64KB. No gfx94x MFMA path touched.\\n- **Control plane is Rust-only.** No JS/TS surface.\\n- **Validation authority:** `docs/VALIDATION.md` (§Retired coherence-gate) — `coherence-gate-dflash.sh` is retired. No universal gate. Claim class == forward numerical parity → path-specific smoke + tau/tok_s measurement (section 5). `cargo check --workspace --all-targets` is the automatic gate.\\n- **Constraints from ticket:** NO SILENT STUBS, no new kernels, `git diff beta` deletions only in `crates/hipfire-arch-muse-glimmer/` + additive daemon hooks, `cargo check --workspace --all-targets` clean, do not commit/fmt.\\n\\n### Reference anchors (frozen)\\n\\n- Upstream: `\\\\`/tmp/ga_modeling.py:296-312\\\\`` (`MuseGlimmerAssistantContextProjection.fc + output_norm_enc`), `\\\\`/tmp/ga_modeling.py:138-196\\\\`` (Attention: `kv_hidden_states = cat([context_hidden_states, hidden_states])`, `q_proj(hidden_states)` -> B, `k/v_proj(kv_hidden_states)` -> ctx+B, `q_norm/k_norm` weighted, `apply_rotary_pos_emb(q,k,cos,sin)` over ctx+B), `\\\\`/tmp/ga_modeling.py:371-444\\\\`` (Model: `context_hidden_states=self.encoder(context_hidden_states)` once, `position_ids=arange(noise_embeds.shape[1]+context_hidden_states.shape[1])+past_seen`, `hidden_states=noise_embeds` NO add, loop `layer(hidden, context_hidden)`).\\n- Local distilled contract: `\\\\`~/.omp/wt/glimmer/local-glimmer-drafter-contract.md:19-65\\\\``\\n- Current divergence table: `local-glimmer-drafter-contract.md:66-83`\\n\\n---\\n\\n## 1. Frozen cross-slice contracts\\n\\n### 1.1 Scratch contract — `crates/hipfire-arch-muse-glimmer/src/drafter.rs:403-475`\\n\\n```rust\\npub struct GlimmerDrafterScratch {\\n    pub x: GpuTensor,                   // [block*hidden]      block=16, hidden=6656\\n    pub target_hidden_proj: GpuTensor,  // [max_ctx*hidden]    max_ctx = ctx_cap (see 1.3)\\n    pub q: GpuTensor,                   // [block*q_dim]       q_dim=4096  (32*128)  Q-only\\n    pub k: GpuTensor,                   // [(max_ctx+block)*kv_dim]  kv_dim=1024 (8*128)  ← restored\\n    pub v: GpuTensor,                   // [(max_ctx+block)*kv_dim]           ← restored\\n    pub attn_out: GpuTensor,            // [block*q_dim]\\n    pub tmp: GpuTensor,                 // [block*hidden]\\n    pub gate_ffn: GpuTensor,            // [block*intermediate]  19968\\n    pub up_ffn: GpuTensor,\\n    pub ffn_hidden: GpuTensor,\\n    pub logits_tmp: GpuTensor,          // [hidden]\\n    pub pos_buf: hip_bridge::DeviceBuffer, // (max_ctx+block)*4 bytes  ← was 4 bytes\\n}\\n// ctor: fn new(gpu:&mut Gpu, cfg:&GlimmerDrafterConfig, max_ctx:usize) -> Result<Self,String>\\n// - max_ctx passed from loader is min(max_seq, HIPFIRE_DFLASH_CTX_CAP|8192) capped to sliding_window (see 1.3)\\n// - alloc: x=block*h, target_hidden_proj=max_ctx*h, q=block*qd, k/v=(max_ctx+block)*kvd, pos_buf=(max_ctx+block)*4\\n// - free_gpu must free all 10 tensors + pos_buf\\n```\\n\\nExisting bug: `drafter.rs:428-435` mallocs 4 bytes; `drafter.rs:408-410` sizes k/v as `block*kvd`. History had `(max_ctx+block)*kvd` and was reduced — restore.\\n\\n### 1.2 Forward contract — `crates/hipfire-arch-muse-glimmer/src/drafter.rs:476-613`\\n\\n```rust\\n// BEFORE (current):\\n//   fn glimmer_drafter_forward(gpu,cfg,weights,scratch,\\n//       noise_embedding:&[f32], target_hidden:&[f32],\\n//       positions_q:&[i32], positions_k:&[i32], block_size, ctx_len)\\n//   where positions_q/k.len()==block, attention L==B, broadcast add, per-row rope on paired Q/K\\n\\n// AFTER (frozen):\\npub fn glimmer_drafter_forward(\\n    gpu: &mut Gpu,\\n    cfg: &GlimmerDrafterConfig,\\n    weights: &GlimmerDrafterWeights,\\n    scratch: &mut GlimmerDrafterScratch,\\n    noise_embedding: &[f32],                 // [block*hidden] raw, no embed_norm\\n    target_hidden: &[f32],                  // [ctx_len * num_extract*hidden]  num_extract=5, 33280/row\\n    positions: &[i32],                      // [ctx_len+block_size] single span, tail is Q positions\\n    block_size: usize,                      // == cfg.block_size ==16\\n    ctx_len: usize,                         // 0..=max_ctx, after capping (see 4)\\n) -> Result<(), String>\\n// Invariants:\\n// - noise_embedding.len()==block*hidden else Err\\n// - target_hidden.len()==ctx_len*5*hidden else Err\\n// - positions.len()==ctx_len+block_size else Err(\\"positions size mismatch\\")\\n// - positions == [cur_pos-ctx_len .. cur_pos+block-1] absolute (continuous). Q positions = positions[ctx_len..]\\n// - cfg.mask_token_id==201818 else Err\\n```\\n\\nSemantics (must match §Reference anchors, no approximation):\\n1. `target_hidden_proj = rmsnorm(fc * target_hidden)` per ctx row ONCE, via `weight_gemv` + `rmsnorm_f32` looping `0..ctx_len` using `scratch.tmp` as staging. `fc` is `WeightTensor [hidden, 5*hidden]` (`encoder.fc.weight`), `output_norm_enc` is `GpuTensor [hidden]`. Reused by all 5 layers (not recomputed per layer).\\n2. `scratch.x = noise_embedding` via `memcpy_htod` ONLY — **remove** `drafter.rs:539-546` broadcast `add_inplace_f32(last_proj)` loop entirely.\\n3. Per-layer (5×):\\n   - `n1 = rmsnorm_batched(x, input_layernorm, tmp, B, h, eps)`  `eps=1e-5`\\n   - `q = q_proj(n1)` per block row (B rows, B*qd)\\n   - `kv_hidden = cat(ctx_proj, n1)` conceptually → `k = k_proj(kv_hidden)` over ctx+B rows, `v = v_proj(kv_hidden)` over ctx+B rows (loop `0..ctx_len` from `target_hidden_proj` slices, `0..B` from `n1` slices into `k`/`v` tail).\\n   - `rmsnorm_batched(q, q_norm, q, B*n_heads, head_dim, eps)` and `rmsnorm_batched(k, k_norm, k, (ctx+B)*n_kv_heads, head_dim, eps)` — weighted per-head (real q/k_norm weights, not scale-less).\\n   - RoPE half-split (`rope_theta=500000`) over concatenated extent: `Q` rows use `positions[ctx_len..]` (B entries), `K` rows use `positions[..]` (ctx+B entries). Implementation may use `rope_f32` with `n_heads_q=0` for ctx-K-only rows and `n_heads_k=0` for Q-only (kernel `kernels/src/rope.hip:7-40` skips loop when count 0, pointer dummy valid). Batch fast-path is `rope_batched_f32` where noted; no new kernel.\\n   - `attention_dflash_f32(&q, &k, &v, &attn_out, B, L=ctx+B, 32, 8, 128)` — see 1.4 for mask.\\n   - remaining block unchanged: `o_proj` per row → `tmp`, `x += tmp`, `rmsnorm_batched(x, post_attention_layernorm, tmp, B, ...)`, `gate/up/down` + `silu_mul_f32` + residual, `x=rmsnorm_batched(x, norm, x, B, ...)` final.\\n4. No other signature change; return `Ok(())` with `scratch.x` holding final normed hidden (caller runs `lm_head` over rows `1..B-1`).\\n\\n### 1.3 Daemon ↔ loader contract\\n\\n- `MuseGlimmerBundle.target_hidden_host: Vec<f32>` in `crates/hipfire-loader/src/lib.rs:373` is `[n_committed * 33280]` host concat in order `[1,13,25,37,49]`. Grown in `daemon.rs:29242` (prefill) and `daemon.rs:29532` (verify), truncated to `hidden_before+keep_rows*33280` at `daemon.rs:29542`.\\n- Loader `carriers.rs:1864` already sizes `GlimmerDrafterScratch::new(ctx.gpu, &dcfg, ctx.max_seq)` with `ctx.max_seq = min(requested_max_seq, 512K)` capped via `HIPFIRE_DFLASH_CTX_CAP|8192` at `daemon.rs:19024`. New bound: `max_ctx = min(ctx.max_seq, cfg.sliding_window)` with `sliding_window=2048` (i.e. 2048). Scratch therefore needs `(2048+16)*1024 ≈2.1M f32` per K/V ≈8 MB each — trivial.\\n- Daemon call site frozen:\\n```rust\\n// file: crates/hipfire-runtime/examples/daemon.rs:29346-29416\\nlet row_elems = ne * hidden; // 33280\\nlet n_rows = bundle.target_hidden_host.len() / row_elems;\\nlet ctx_cap: usize = drafter.config.sliding_window; // 2048 — or sliding_window.saturating_sub(block_size) =2032 (choose one and document)\\nlet ctx_len = n_rows.min(ctx_cap);\\nlet start_elem = (n_rows - ctx_len) * row_elems; // suffix (most recent window)\\nlet target_hidden: Vec<f32> = bundle.target_hidden_host[start_elem..].to_vec(); // [] when n_rows==0\\nlet cur_pos = bundle.state.n_tokens as i32; // absolute position of seed (= next write slot)\\nlet positions: Vec<i32> = (cur_pos - ctx_len as i32 .. cur_pos + block_size as i32).collect(); // len ctx+B, tail is Q\\nlet ok = glimmer::drafter::glimmer_drafter_forward(gpu, &drafter.config, &drafter.weights, &mut drafter.scratch,\\n    &noise_embedding, &target_hidden, &positions, block_size, ctx_len);\\n```\\n- Env: `HIPFIRE_DFLASH_DRAFT` selects draft .hfq, `HIPFIRE_GLIMMER_SPEC_DIAG=1` enables l2 diag, `HIPFIRE_GLIMMER_TAP_LAYERS` selects tap layers — unchanged. `HIPFIRE_DFLASH_CTX_CAP` may further cap `max_ctx`.\\n\\nChosen bound to report: **`ctx_len = min(n_accepted, sliding_window)` with suffix truncation (keep most recent 2048 rows), i.e. 2048**. Alternative conservative `2032 = 2048-block` is also correct (ensures `ctx+B ≤2048` so windowed==full). Either is acceptable if documented; prefer **2048** (full window, drop oldest) because upstream window is 2048 and `ctx+B=2064` overflow is 16 tokens — negligible. **State the bound explicitly in the tape log** (`eprintln!(\\"[glimmer-spec] ctx {}\\\\/{}\\", ctx_len, ctx_cap)`).\\n\\n### 1.4 Attention mask contract\\n\\n- `attention_dflash_f32` at `crates/rdna-compute/src/attention.rs:6778` + `kernels/src/attention_dflash.hip:33-40` explicitly supports `B != L` (`q: [B*nh*hd], k/v: [L*nkv*hd], out: [B*nh*hd], Grid [n_heads, B]`) with tiled online-softmax, non-causal (bidirectional) — exactly the needed \\"Q=B against K/V=ctx+B bidirectional\\" contract. No causal mask, GQA 32/8, hd 128, f32.\\n- Sliding window 2048: `attention_dflash_f32` is **full** bidirectional, not windowed. No existing helper expresses `B` vs `ctx+B` **and** a 2048 sliding window bidirectionally without a new kernel. Nearest windowed helpers are `attention_q8_0_kv_swa`/`attention_q8_0_flash_prefill_wmma` but they require Q8 quantized KV and/or causal decode shape — rejected for this DFlash f32 path (see `drafter.rs:42-57` rationale). Therefore:\\n  - For `ctx+B ≤2048` (or `≤2032` if bounding there) the full kernel is **exact** (window == full).\\n  - For larger `ctx` the full kernel over-attends to distant context by at most `ctx+B-2048 ≤~16` tokens if capping at 2048. This is the documented approximation. **Do not silently fall back to `L==B`; do not add a new kernel; do state the approximation in code comment.**\\n  - If no helper could express `B != L`, the plan would STOP — not applicable here because `attention_dflash_f32` does.\\n\\n---\\n\\n## 2. Slice decomposition (composer-owned, parallelizable)\\n\\n### Slice A — `GlimmerDrafterScratch + glimmer_drafter_forward` (single file, one owner)\\n\\n**Owner:** backend-kernel (hipfire-arch-muse-glimmer)\\n**Target:** `crates/hipfire-arch-muse-glimmer/src/drafter.rs`\\n**Non-goals:** daemon, loader, any other crate.\\n\\n**Change — step by step:**\\n- `drafter.rs:1-68` header comment: replace broadcast/L==B paragraph (`Extent decision` at `drafter.rs:33-40`) with concatenated contract: ctx_proj once, K/V=ctx+B, Q=B, positions span ctx+B, attention_dflash_f32 B vs L, window approximation note.\\n- `drafter.rs:405-425` struct: `q: [B*qd]` stays, `k/v: [(max_ctx+block)*kvd]` (restore), `pos_buf: DeviceBuffer` comment update to `(max_ctx+block)*4`.\\n- `drafter.rs:427-455` `Scratch::new`: `max_ctx` param already exists via `carriers.rs:1864` (`ctx.max_seq`). Compute `qd/kvd/block/h`, `alloc(\\"q\\", block*qd)`, `alloc(\\"k\\", (max_ctx+block)*kvd)`, `alloc(\\"v\\", (max_ctx+block)*kvd)`, `pos_buf = hip.malloc((max_ctx+block)*4)`, plus existing `target_hidden_proj = max_ctx*h`, etc. Keep `free_gpu` matching.\\n- `drafter.rs:484-613` `glimmer_drafter_forward`:\\n  - Signature: replace `positions_q:&[i32], positions_k:&[i32]` with `positions:&[i32]` (single span) as frozen in 1.2; update doc comment `noise_embedding`/`target_hidden`/`positions` accordingly. Keep `block_size, ctx_len` trailing args.\\n  - Validate `positions.len()==ctx_len+block_size` (error string must mention expected vs got), `target_hidden.len()==ctx_len*5*hidden`, `noise_embedding.len()==block*hidden`, `mask_token_id==201818`.\\n  - Keep `drafter.rs:521-533` per-row `fc` loop over `0..ctx_len` (host upload into `tmp.sub_offset(0, ne*h)` → `weight_gemv(fc, tmp_in, target_hidden_proj.row)` → `rmsnorm_f32(output_norm_enc)`) — this already is the real GPU path over n_ctx rows (not the described host shortcut). Ensure it remains and is not replaced by a host matmul. If any `ctx_len==1` fast-path exists, delete it.\\n  - Delete `drafter.rs:539-546` broadcast add entirely (the `last_proj` loop). `x` is noise alone.\\n  - Per-layer loop `drafter.rs:548-609`:\\n    * `rmsnorm_batched(x, input_layernorm → tmp, B)` unchanged.\\n    * Projection: `q` over B rows from `tmp`; `k/v` over ctx+B rows: first `ctx_len` rows from `target_hidden_proj` slices, remaining `B` rows from `tmp` slices. Use two sub-loops per layer for `k_proj`/`v_proj` (ctx then block tail). Sizes now match restored scratch.\\n    * Norms: `rmsnorm_batched(q, q_norm, q, B*nh, hd)`, `rmsnorm_batched(k, k_norm, k, (ctx+B)*nkv, hd)` — note k batch is ctx+B, not B.\\n    * RoPE: implement as described in 1.2. Host builds `positions` already (daemon), but inside forward we must upload to `pos_buf` and call roping. For tail: loop `i in 0..B` with `pos=positions[ctx_len+i]`, `q_row = q.sub_offset(i*qd,qd)`, `k_tail_row = k.sub_offset((ctx_len+i)*kvd,kvd)` → `rope_f32(&q_row, &k_tail_row, &pos_buf_single, n_heads, n_kv_heads, hd, theta)` where `pos_buf_single` is a 1-int DeviceBuffer slice (`pos_buf` offset `i*4`? existing impl uses 1-int `pos_buf` per call via `memcpy_htod`). For ctx head: either per-row `rope_f32` with `n_heads_q=0` (q dummy) or one `rope_batched_f32` for ctx batch (preferred if feasible without new alloc). Document choice and note that per-row ctx loop is ≤2048×5 launches — acceptable for 48-token smoke, batch optimization is follow-up.\\n    * Attention: `gpu.attention_dflash_f32(&q, &k, &v, &attn_out, B, ctx_len+B, 32,8,128)` — note `L = ctx+B`, not `B`.\\n    * Rest (o_proj, residuals, FFN, final norm) unchanged except they read `tmp`/`x` which now hold correct attention output.\\n  - Update comment at `drafter.rs:573-585` to reflect `L = ctx+B` and window note.\\n- Add `#[cfg(test)]` unchanged (`qkv_dims_match_config` at `drafter.rs:615-640`), optionally add `positions_len_mismatch` test for new invariant.\\n\\n**Acceptance (slice A):**\\n- `cargo check --workspace --all-targets` clean.\\n- `git diff beta --stat` shows changes only inside `crates/hipfire-arch-muse-glimmer/` for this slice.\\n- Scratch `k`/`v` capacity equals `(max_ctx+block)*kvd`; `pos_buf` capacity `(max_ctx+block)*4`; `q` stays `block*qd`.\\n- `glimmer_drafter_forward` no longer contains `add_inplace_f32(&dst,&last_proj)` broadcast; `attention_dflash_f32` call uses `l = ctx_len+block` and `k`/`v` are `ctx+B` sized.\\n- Unit test `positions size mismatch` trips when `positions.len()!=ctx+B`.\\n\\n**Measure gate:** local `cargo check` only; perf is slice B\'s gate.\\n**Abandon:** if `k/v` allocation for `max_ctx=8192` OOMs on 9070 (unlikely: 8192*1024≈8M f32≈33 MB per buffer), cap `max_ctx` to `min(max_ctx, sliding_window)` and retry — not abandon.\\n\\n### Slice B — Daemon `generate_muse_glimmer` context + positions (single file, one owner)\\n\\n**Owner:** runtime-daemon\\n**Target:** `crates/hipfire-runtime/examples/daemon.rs` — inside `fn generate_muse_glimmer` (`daemon.rs:29030` via search `glimmer_drafter_forward`)\\n**Non-goals:** drafter crate, loader, other generate paths (gemma4/lfm2moe remain AR).\\n\\n**Change — step by step:**\\n- Locate block at `daemon.rs:29392-29416` (comment `Target hidden history: last ctx row`, `let mut target_hidden = if ... last row ..`, `let ctx_len: usize =1`, `let positions_q/k: Vec<i32> = (cur_pos .. cur_pos+block)`, `glimmer_drafter_forward(..., &positions_q, &positions_k, block, ctx_len)`).\\n- Replace with:\\n```rust\\nlet ne = drafter.config.num_extract(); //5\\nlet hidden = drafter.config.hidden; //6656\\nlet row_elems = ne*hidden; //33280\\nlet n_rows = bundle.target_hidden_host.len() / row_elems;\\nlet ctx_cap = drafter.config.sliding_window; // 2048 — document bound; alternative 2032=2048-block\\nlet ctx_len = n_rows.min(ctx_cap);\\nlet target_hidden: Vec<f32> = if ctx_len==0 { Vec::new() } else {\\n    let start = (n_rows - ctx_len)*row_elems;\\n    bundle.target_hidden_host[start..].to_vec() // suffix, keep most recent\\n};\\nlet positions: Vec<i32> = (cur_pos as i32 - ctx_len as i32 .. cur_pos as i32 + block_size as i32).collect();\\n// assert positions.len()==ctx_len+block_size\\nlet ok = glimmer::drafter::glimmer_drafter_forward(gpu, &drafter.config, &drafter.weights, &mut drafter.scratch,\\n    &noise_embedding, &target_hidden, &positions, block_size, ctx_len);\\n```\\n  Remove `positions_q/k` block-sized vectors and `ctx_len=1` hardcode.\\n- Update diag at `daemon.rs:29448-29479` (`HIPFIRE_GLIMMER_SPEC_DIAG`) to report `ctx_len` and `positions` head/tail: `eprintln!(\\"[glimmer-spec] ctx {}/{} cur_pos {} pos head {:?} tail {:?}\\", ctx_len, ctx_cap, cur_pos, &positions[..2.min], &positions[positions.len()-2..])` and existing `target_hidden` l2 already logs — add `target_hidden` rows count.\\n- Keep `total_proposed/accepted/windows` tau logging at `daemon.rs:29545-29549` unchanged (now denominator grows because drafts are conditioned, not zero).\\n- No other daemon paths (AR fallback `daemon.rs:29629`, verify `daemon.rs:29532` with `verify_block_with_capture` and truncate) — keep as is; truncation already preserves `target_hidden_host` suffix invariant.\\n- `git diff beta` for this slice is additive daemon hooks only (no deletions outside glimmer crate).\\n\\n**Acceptance (slice B):**\\n- Same `cargo check` clean.\\n- On a 48-token smoke (`/tmp/glimmer-smoke.py x 48` via `ssh hiptrx … HIP_VISIBLE_DEVICES=3 … timeout 500 … grep -E \\"done:|drafts sample\\" /tmp/gdiag3.log`), `ctx_len` grows across windows (not stuck at 1), `positions.len()==ctx_len+16`, `target_hidden.len()==ctx_len*33280`.\\n- No `ctx_len==1` hardcode remains (`grep -n \\"ctx_len.*1\\" daemon.rs` shows only the bound).\\n\\n**Measure gate:** hardware smoke — see §5.\\n**Abandon:** if `bundle.target_hidden_host` is observed empty after prefill (l2 0), the capture path `decode_step_with_capture` at `daemon.rs:29230-242` is broken — fix capture before claiming drafter fix; not a drafter logic abandon.\\n\\n---\\n\\n## 3. Call sites, flags, env, and build order\\n\\n**Call sites:**\\n- `drafter.rs:485` `glimmer_drafter_forward` — sole caller is `daemon.rs:29412` (`glimmer_drafter_forward` search). Also potential test harness; update all.\\n- `GlimmerDrafterScratch::new` — sole caller `carriers.rs:1864` (`ctx.max_seq`), already passes `max_ctx` correctly.\\n\\n**Flags/env (no new env):**\\n- `HIPFIRE_DFLASH_DRAFT=/home/kaden/.hipfire/models/muse-glimmer-30b-assistant.q8.hfq` (draft selector)\\n- `HIPFIRE_GLIMMER_SPEC_DIAG=1` (per-stage l2)\\n- `HIPFIRE_GLIMMER_TAP_LAYERS=0,12,24,36,48` etc (tap A/B, not changed)\\n- `HIPFIRE_GLIMMER_SPEC_PERTURB=1` (perturb check), `HIPFIRE_GLIMMER_SPEC_FALLBACK=1` (fallback to AR)\\n- `HIPFIRE_DFLASH_CTX_CAP|HIPFIRE_JINJA_CHAT` — capped max_ctx\\n\\n**Ordered build sequence (do not reorder):**\\n1. Apply Slice A (`drafter.rs`) first — it changes the API surface. `cargo check --workspace --all-targets` must pass with Slice A alone (daemon will not compile until Slice B updates the call site — so check Slice A with `cargo check -p hipfire-arch-muse-glimmer` only).\\n2. Apply Slice B (`daemon.rs`) to match new `positions` signature. Then `cargo check --workspace --all-targets` clean end-to-end.\\n3. `cargo build --release --features deltanet --example daemon -p hipfire-runtime` on hiptrx (worktree `\\\\`/home/kaden/wt-glimmer\\\\``, branch `muse-glimmer`, device 3). Use `scp` of changed files before build (edits are local, parent commits).\\n4. Smoke run (see §5) — do not run project-wide test suites/formatters (caller does shared validation).\\n\\n---\\n\\n## 4. What we are NOT doing (explicit)\\n\\n- No new HIP kernel. RoPE batching reuses `rope_f32`/`rope_batched_f32` (`rdna-compute/src/norm.rs:763`, `823`) and `attention_dflash_f32` (`attention.rs:6778`).\\n- No change to `target_hidden` capture (`forward.rs:468-513` `decode_step_with_capture`, `verify_block_with_capture:519`) — already verified healthy (`l2 665.29`, 33280 non-zero) and truncation at `daemon.rs:29542`.\\n- No tap-layer re-indexing — A/B on hardware `[1,13,25,37,49]` vs `[0,12,24,36,48]` already identical tau, not bottleneck.\\n- No change to `q_norm/k_norm` weighting, `norm_eps 1e-5`, `rope_theta 500000`, `mask_token_id 201818` — already fixed.\\n- No shared-code deletions outside `crates/hipfire-arch-muse-glimmer/`.\\n- No safetensors/Dir load for glimmer (`carriers.rs:1909` remains unwired).\\n- No Redline/PM4/AQL tape work.\\n\\n---\\n\\n## 5. Measurement gates & abandon criteria (claim-scoped, VALIDATION.md §Claim→route)\\n\\n**Automatic gate:** `cargo check --workspace --all-targets` clean.\\n\\n**Manual GPU gates (hiptrx, device 3 only, 0/1/2 in use):**\\n\\nBuild:\\n```\\ncd /home/kaden/wt-glimmer && git fetch -q origin muse-glimmer && git checkout -q -f FETCH_HEAD\\n# scp local drafter.rs + daemon.rs into worktree, then\\ncargo build --release --features deltanet --example daemon -p hipfire-runtime\\n```\\nRun (single fresh-process, then triplicate if promising):\\n```\\nssh hiptrx \'cd /tmp && HOME=/home/kaden/glimmer-home-3 GLIMMER_DAEMON=/home/kaden/wt-glimmer/target/release/examples/daemon GLIMMER_PROMPT_FILE=/tmp/glimmer_prompt.txt GLIMMER_ERRLOG=/tmp/gdiag3.log HIP_VISIBLE_DEVICES=3 HIPFIRE_DFLASH_DRAFT=/home/kaden/.hipfire/models/muse-glimmer-30b-assistant.q8.hfq timeout 500 python3 /tmp/glimmer-smoke.py x 48 >/dev/null 2>&1; grep -E \\"done:|drafts sample|glimmer-spec.*tau|glimmer-diag|ctx\\" /tmp/gdiag3.log | tail -30\'\\n```\\nWith `HIPFIRE_GLIMMER_SPEC_DIAG=1` for first 2 windows (l2 diag), without for perf.\\n\\n**Metrics to report (≥3 fresh-process runs, record prompt md5 + binary md5, eyeball decoded output):**\\n- `tau = (total_accepted + windows)/windows` (bonus counted, same as `daemon.rs:29626`). Baseline to beat: **1.016-1.022, ~1/700 accepts**.\\n- `tok/s` AR vs DFlash from same prompt (AR is `use_spec=false` path at `daemon.rs:29629`; DFlash is `use_spec=true` at `daemon.rs:29288`). Report both.\\n- Byte-identical at temp 0: `HIPFIRE_GLIMMER_SPEC_PERTURB=1` must show `PERTURB CHECK OK` (forcing `draft[0]=pick[0]` changes accept) — proves comparison sensitive and drafter ran. Greedy argmax divergence without perturb is a correctness bug, not tau.\\n- Sanity: `target_hidden l2` ~665, `noise_embedding` non-zero, `draft hidden row` l2 finite, `drafts sample` varies per window (not constant 0).\\n\\n**Success:** any statistically stable tau lift over 1.022 is meaningful; tau ≥2 is competitive. Even modest 1.2-1.5 with stable tok/s lift is a valid improvement to ship default-OFF behind env if not bit-exact? But this change is bit-exact by construction (no reordering beyond tiled attention\'s 1e-8), so default ON is allowed once tau lifted.\\n\\n**Honest negative (valid result):** if after correct contract (verified by code review: no broadcast, K/V=ctx+B, positions span ctx+B, ctx_len grows, attention L=ctx+B) tau remains ~1.02 and `drafts sample` varies, report plainly with evidence (3 runs, prompt md5, binary md5, tau, tok/s, decoded output eyed). Best evidence-backed reading of why (e.g., tap-layer shift already A/B\'d identical; remaining suspects: target_hidden capture order, qk_norm weight convention, or training-vs-inference mismatch — but do not re-investigate beyond stating).\\n\\n**Abandon / STOP:**\\n- If no existing attention helper can express `Q=B` vs `K/V=ctx+B` **and** bidirectional mask without a new kernel, STOP and report rather than silently falling back to `L==B`. Not triggered — `attention_dflash_f32` at `attention.rs:6778` + `kernels/src/attention_dflash.hip:10-17` does express it (tiled, GQA, f32, non-causal). Sliding window 2048 is the only unexpressed nuance — document as full-attention approximation (exact when `ctx+B ≤2048`).\\n- If per-row RoPE for ctx (≤2048 rows) causes >50% wall-time regression on 48-token smoke (profile `attention_dflash_f32` vs RoPE time), batch RoPE into `rope_batched_f32` before claiming perf — not abandon, just optimize.\\n\\n---\\n\\n## 6. Risks & mitigations\\n\\n- **Pos_buf lifetime** (`drafter.rs:424` was malloc 4 then freed per-layer, fixed to persistent) — keep persistent, free in `free_gpu` only.\\n- **Weight_gemv staging** (`drafter.rs:524-531` uses `tmp.sub_offset`) — ensure `tmp` is `block*hidden` (≥33280) so `ne*h` fits for fc staging (33280). It does.\\n- **Silent truncation:** daemon must suffix-truncate oldest ctx rows when `n_rows > ctx_cap`, not prefix — otherwise recent context lost. Log `ctx_len` each window.\\n- **Determinism:** tiled `attention_dflash_f32` multi-tile carries `(max,sum,out)`; single-tile vs multi-tile differ by FP order <1e-7 — acceptable for greedy argmax.\\n\\n---\\n\\n```mermaid\\nflowchart LR\\n  subgraph daemon[\\"daemon.rs generate_muse_glimmer\\"]\\n    TH[\\"target_hidden_host [n*33280]\\"] -->|suffix ctx_cap=2048| TH2[\\"target_hidden [ctx*33280]\\"]\\n    NP[\\"noise_embedding [16*6656] raw\\"] --> D\\n    POS[\\"positions [ctx+16]\\"] --> D\\n  end\\n  subgraph drafter[\\"drafter.rs glimmer_drafter_forward\\"]\\n    D -->|fc + output_norm_enc ONCE| CTP[\\"ctx_proj [ctx*6656]\\"]\\n    CTP -->|cat per layer| K[\\"k/v [ctx+16 x kvd]\\"]\\n    NP -->|input_layernorm| Q[\\"q [16 x qd]\\"]\\n    Q & K -->|rope over ctx+16| R\\n    R -->|attention_dflash_f32 B=16 L=ctx+16 bidir| A[\\"attn_out\\"]\\n    A --> FFN --> X[\\"x [16*6656] final norm\\"]\\n  end\\n  X -->|lm_head slice 1..| LOGITS[\\"drafts [15]\\"]\\n  LOGITS --> VERIFY[\\"verify_block_with_capture\\"]\\n```\\n"\n}', 'startLine': 1, 'lineNumbers': [1, 2, 3]}, 'meta': {'source': {'type': 'internal', 'value': 'agent://GlimmerDraftCtx'}}}}