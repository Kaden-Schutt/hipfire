# DeepSeek V4 Flash 0731 MQ2RXT v1 retained-PM4 lowering

Date: 2026-08-05  
Branch: `ds4-beta-staging`  
Corrected commit: `29e210a0f855bbc666abc218be8fc66f9c58d583`

## Result

MQ2RXT is lowerable to a correct retained-PM4 route on gfx1151. The first
route exposed one uncaptured raw HIP launch per transformer layer. After that
launch was migrated to `launch_maybe_blob`, the fresh tape passed 15-position
logits, KV-cache, and recurrent-state parity and produced byte-identical output
on the committed short code fixture.

The matched one-fresh-process product screen was:

| Arm | Decode | Output MD5 |
|---|---:|---|
| Explicit ordinary HIP | 28.0702 tok/s | `a1149e82dd99208082c9de734efc7254` |
| Explicit retained PM4 | 29.0052 tok/s | `a1149e82dd99208082c9de734efc7254` |
| PM4 / HIP | **1.03331x (+3.331%)** | byte-identical |

This is a diagnostic screen (`n=1` fresh process per arm), not a stationary
acceptance median. It establishes that the new route works and has positive
transport headroom; it does not yet authorize automatic `.mq2rxt` admission.

Strategically, this first untuned MQ2RXT artifact has already reached and
slightly exceeded the recertified MQ2R retained-PM4 golden line (28.8678
tok/s on the same short-code regime). MQ2RXT PM4 is +0.1374 tok/s (+0.48%)
over that line before any MQ4-specific kernel or route tuning. It therefore
one-shots the throughput reached by the multi-week MQ2R dense-E8SOA campaign;
the E8SOA result is a comparison baseline, not a ceiling or a reason to assume
the MQ4-dense construction has less headroom.

## Artifact and fixture identity

- Local hipx artifact:
  `/home/kaden/models/ds4-mq2rxt-v1/deepseek-v4-flash-0731.mq2rxt`
- Artifact SHA-256:
  `131a0f4fecb3bcf1ff1f00fbe29dc065a51cd82a2bdba3781f4ba827c6c38037`
- Size: 82,147,464,366 bytes
- Recipe: frozen MQ2R P3 tensor map, with all 554 qt=35 dense tensors replaced
  from the native 0731 parent by qt=13 MQ4G256; routed experts remain qt=19
  MQ2G256Lloyd.
- Tensor census: qt1=641, qt3=1, qt13=554, qt19=33,024, qt22=3, qt35=0.
- Prompt: `benchmarks/prompts/ds4_dspark_genre_code.json`, entry 0
- Prompt-content MD5: `d782138f5bc8bbbd234ca8e4b17cace9`
- 25 prompt tokens, 128 generated tokens, batch 1, greedy, Q8 request mode,
  six experts per token, thinking off, speculation off.
- GPU: gfx1151, HIP 7.14, automatic clocks.
- Corrected daemon SHA-256:
  `c571275ddd2ee5f7b67af996c64cc9ec32c92555869a75786378e71fae3d655f`
- Corrected CLI SHA-256:
  `d22beb4d3eb64bbfc27f43d4f797441383515abd6f816b1988256c62cdb4f03e`

The artifact was copied from the NAS to local hipx storage before these runs.
The earlier multi-minute load was a NAS-path effect; the local copy loaded in
the normal sub-minute range. Both source and local copies were previously
verified against the artifact SHA above.

## Failure and root cause

Before `29e210a0f`, the captured route was stable but incomplete:

- 2,277 launches
- 31 unique symbols
- sequence hash `e44a32364a36c5a5`
- 31/31 AQL contracts, zero fallbacks
- ordinary retained AQL and retained PM4 both failed parity
- captured-HIP blob, AQL, and PM4 agreed with each other but disagreed with
  the ordinary-HIP forward
- conservative PM4 waits/acquires failed identically, ruling out the lean
  resource wait policy

The exact sequence diff against the passing MQ2R tape showed 43 missing
launches. MQ2RXT uses `wo_per_group_batched_hfq4g256` once per transformer
layer for grouped O-LoRA `wo_a`. That helper still called
`self.hip.launch_kernel` directly, so Redline never recorded it. Ordinary HIP
therefore remained coherent while every retained transport replayed a tape
missing one material projection per layer.

Commit `29e210a0f` migrates only that helper to `launch_maybe_blob` with an
explicit seven-field kernarg blob. No kernel arithmetic, weight, format,
Qwen path, replay policy, or gfx1100 policy changed.

## Corrected route proof

The corrected `scripts/redline_daemon_harness.py --pm4` result:

- stable capture: 2/2
- 2,320 launches
- 32 unique symbols
- sequence hash `ec2ff8a618b74f30`
- 32/32 Radiowave/AQL contracts
- certified launches: 2,320; fallback launches: 0; unknown launches: 0
- 2,319/2,319 resource boundaries covered
- one retained PM4 packet on queue 3
- manual-shadow command stream: 57,921 dwords
- 15 consecutive positions
- logits exact: yes
- KV-cache exact: yes
- recurrent state exact: yes
- captured-HIP blob exact: yes

The product serving route reported the same 2,320/32/`ec2ff8a618b74f30`
launch sequence, one packet on queue 3, and 57,920 dwords. The one-dword
difference is between the manual-shadow and product preparation surfaces; it
does not change the ordered launch sequence. Each surface is internally pinned
by its own evidence.

## Product output

Both product arms emitted the same coherent Python merge-function response.
The response ended at the deliberate 128-token cap, hence `finish=length`; it
was neither repetitive nor an attractor. Both outputs were 519 bytes with MD5
`a1149e82dd99208082c9de734efc7254`, which also matches the earlier MQ2RXT
ordinary-HIP smoke.

Automatic `.mq2rxt` retained-PM4 admission remains off. The product PM4 arm
used the explicit certification opt-in, while the HIP arm explicitly disabled
retained replay. A repeated/stationary promotion run and the normal serve
battery remain required before changing the product default.

## DSpark disposition

The separately produced MQ2RXT DSpark sidecar is not promoted. On the same
short code fixture it measured 27.6057 tok/s with tau 1.886, slower than its
own AR path, and did not reproduce the AR output bytes. No DSpark PM4 work was
performed as part of this lowering.

## Evidence

Evidence root on hipx:

`/home/kaden/ds4-gfx1151-evidence/2026-08-05-mq2rxt-v1/`

Key records:

- Corrected shadow:
  `redline-shadow15-fixed/redline-shadow15.json`
  (`666893393750bb56c94a495232b4009dd896bd55a865e55c8b67576d73f63d5e`)
- Corrected shadow log:
  `redline-shadow15-fixed/redline-shadow15.log`
  (`171722ceff7599bda88d0382cee0ffb3dd912f192cb4266c741f38f08817d77a`)
- Matched HIP result:
  `golden-ar-direct-hip-matched/results.json`
  (`76f5067406969dd361620a69393ec442349fde1f43dd34530ee72f29067b5a97`)
- Matched HIP log:
  `golden-ar-direct-hip-matched/serve.log`
  (`c2ee67906d1bed144586166f428b1cdf8464f8daa187a4bf3364874bc2bd1bd3`)
- Retained-PM4 result:
  `golden-ar-retained-pm4/results.json`
  (`6a65963a49e03ae80bfa41b5006d24c2a60193098fd1b2ef58c477a16cdb1824`)
- Retained-PM4 log:
  `golden-ar-retained-pm4/serve.log`
  (`c61fe4f5af95103eaf2713e818906811795c3a851707fbd53aaac33aed6f2da6`)
- Preserved pre-fix failures:
  `redline-shadow15/`, `redline-shadow15-conservative/`, and `aql-shadow15/`.

## Skipped

- No automatic `.mq2rxt` PM4 admission.
- No three-sample/stationary acceptance benchmark.
- No 2,048/512 product claim.
- No long-context route certification.
- No DSpark PM4 lowering.
- No Qwen or gfx1100 GPU run because the correction changes a DS4-only helper
  call site and does not touch replay policy or a Qwen-owned function body.
