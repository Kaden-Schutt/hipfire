[package]
name = "hipfire-mixer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Per-layer token-mixer taxonomy and sequence-state shape for hipfire (the family-seam Mixer model)"

[dependencies]
serde.workspace = true
