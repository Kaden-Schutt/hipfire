[package]
name = "hipfire-arch-minimax-spec"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lean offline spec for the MiniMax-M2 MoE family (arch_id 10): identity + Ingest quant-policy, no runtime/kernel deps."

[dependencies]
hipfire-arch-api = { path = "../hipfire-arch-api" }
