[package]
name = "hipfire-state"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sequence-state handles, descriptors, and reservation helpers for hipfire"

[dependencies]
hipfire-model = { path = "../hipfire-model" }
serde.workspace = true
serde_json = "1"
