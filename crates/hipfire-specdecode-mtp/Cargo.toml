[package]
name = "hipfire-specdecode-mtp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MTP (Multi-Token Prediction) speculative-decode strategy (generic over SpecDecodeTarget)"

[dependencies]
hipfire-specdecode = { path = "../hipfire-specdecode" }
hip-bridge = { path = "../hip-bridge" }
hipfire-runtime = { path = "../hipfire-runtime" }
hipfire-rdna = { path = "../hipfire-rdna" }
