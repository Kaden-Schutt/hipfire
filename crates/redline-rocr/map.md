# redline-rocr — map

> **Status:** `research` (vocabulary owned by [`docs/GLOSSARY.md`](../../docs/GLOSSARY.md)).
> **Layer:** Retained replay / Device FFI — public ROCr/HSA ABI + PM4/AQL builders that `redline-dispatch::aql` replays through; see the layering table in [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md).

## Purpose

Low-level public ROCr/HSA runtime and AQL packet mechanics. Owns the dynamically loaded ROCr ABI (`abi.rs` — transcribed from MIT ROCm 7.2 public headers `/opt/rocm/include/hsa/{hsa.h,hsa_ext_amd.h}`, commit `c0430a5…`), resource lifetimes, architected 64-byte AQL packet encoding (`packet.rs` — `KernelMetadata`, `LaunchGeometry`), and release-ordered queue publication + PM4 builders (`pm4.rs`/`pm4_gfx10.rs`). The `//!` docs in [`src/lib.rs`](src/lib.rs) stress no dispatch-DAG policy lives here.

## Gotchas

- ABI values are from public headers — no ROCm source is vendored; re-transcribe from the same headers on upgrade, don't copy `hsaco` internals.
- 64-byte AQL packets are architected — mis-aligned or partially written packets corrupt the queue's release semantics.
- Only 64-bit little-endian hosts are supported (`compile_error!` guard in `lib.rs`).

## Crate map

<!-- crate-map:generated:begin -->

_Generated by `scripts/check-crate-maps.py` from the tree — do not edit inside the markers._

### Modules

| File | Lines | Public items | Tests |
|---|---:|---:|---:|
| [`src/abi.rs`](src/abi.rs) | 454 | 114 | 0 |
| [`src/lib.rs`](src/lib.rs) | 111 | 7 | 0 |
| [`src/packet.rs`](src/packet.rs) | 776 | 10 | 6 |
| [`src/pm4.rs`](src/pm4.rs) | 744 | 3 | 9 |
| [`src/pm4_gfx10.rs`](src/pm4_gfx10.rs) | 1,012 | 7 | 13 |
| [`src/runtime.rs`](src/runtime.rs) | 3,011 | 18 | 12 |

### Public API surface

- [`src/abi.rs`](src/abi.rs): `Status`, `SignalValue`, `STATUS_SUCCESS`, `STATUS_INFO_BREAK`, `SYSTEM_INFO_TIMESTAMP_FREQUENCY`, `DEVICE_TYPE_CPU`, `DEVICE_TYPE_GPU`, `AGENT_INFO_NAME`, `AGENT_INFO_PROFILE`, `AGENT_INFO_DEFAULT_FLOAT_ROUNDING_MODE`, `AGENT_INFO_WORKGROUP_MAX_DIM`, `AGENT_INFO_WORKGROUP_MAX_SIZE`, +102 more
- [`src/lib.rs`](src/lib.rs): `abi`, `packet`, `pm4`, `pm4_gfx10`, `runtime`, `load_symbols`, `LoadError`
- [`src/packet.rs`](src/packet.rs): `AQL_PACKET_BYTES`, `BARRIER_DEPENDENCY_CAPACITY`, `FenceScope`, `HeaderPolicy`, `LaunchGeometry`, `KernelMetadata`, `KernelDispatchPacket`, `BarrierAndPacket`, `PacketImage`, `PacketError`
- [`src/pm4.rs`](src/pm4.rs): `Gfx12Pm4CommandBuffer`, `Pm4BuildError`, `Pm4DispatchSpanAttribution`
- [`src/pm4_gfx10.rs`](src/pm4_gfx10.rs): `Gfx10KernelImage`, `Gfx10DispatchInitiatorPolicy`, `Gfx11DispatchInterleave`, `Gfx11ComputeResourceLimitsPolicy`, `Gfx10Pm4CommandBuffer`, `Gfx11Pm4CommandBuffer`, `Gfx10Pm4BuildError`
- [`src/runtime.rs`](src/runtime.rs): `DEFAULT_WAIT_TIMEOUT`, `Runtime`, `PciBusId`, `PciBusIdParseError`, `GpuSelector`, `GpuDevice`, `CompletionSignal`, `QueueDepthSample`, `QueueDepthStats`, `QueueDepthReport`, `AqlQueue`, `QueueSet`, +6 more

### Dependencies (from `Cargo.toml`)

- path: `hipfire-config`
- external: `libloading`
- dev: `proptest`
- build: —

### Reverse dependencies

- workspace crates with a path dependency on this crate: `hip-bridge`, `rdna-compute`, `redline-dispatch`

### Totals

- 6 modules · 6,108 lines · 159 public items · 40 tests · 1 examples

<!-- crate-map:generated:end -->
