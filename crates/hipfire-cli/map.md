# hipfire-cli — map

> **Status:** `production` (vocabulary owned by [`docs/GLOSSARY.md`](../../docs/GLOSSARY.md)).
> **Layer:** Operator — native control plane and HTTP service, above the daemon; see the layering table in [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md).

## Purpose

Native operator for `hipfire run` (tag → `~/.hipfire/models/` path, forced-local policy, one-shot daemon spawn vs `HTTP POST /v1/chat/completions` when serve is up). Owns `hipfire run/serve/pull/diag` surfaces, typed config/registry integration, and long-running serve/attach state. The `//!` docs in [`src/main.rs`](src/main.rs) are authoritative; this binary never shells out to JS/TS.

## Gotchas

- Forced-local gate (`HIPFIRE_LOCAL=1`, `--kv-mode`, `--json`, `--no-stream`) — an HTTP failure while serve is live must abort, not silently spawn a local daemon that collides on VRAM.
- Registry fetch preserves `fresh cache → network → stale cache → bundled` order; a malformed registry is rejected wholesale.

## Crate map

<!-- crate-map:generated:begin -->

_Generated by `scripts/check-crate-maps.py` from the tree — do not edit inside the markers._

### Modules

| File | Lines | Public items | Tests |
|---|---:|---:|---:|
| [`src/bench_concurrency.rs`](src/bench_concurrency.rs) | 892 | 22 | 9 |
| [`src/main.rs`](src/main.rs) | 7,831 | 0 | 40 |
| [`src/serve/complete.rs`](src/serve/complete.rs) | 4,708 | 0 | 60 |
| [`src/serve/http.rs`](src/serve/http.rs) | 674 | 0 | 0 |
| [`src/serve/mod.rs`](src/serve/mod.rs) | 1,673 | 3 | 11 |
| [`src/serve/slots.rs`](src/serve/slots.rs) | 258 | 0 | 0 |
| [`src/setup.rs`](src/setup.rs) | 1,527 | 0 | 16 |

### Public API surface

- [`src/bench_concurrency.rs`](src/bench_concurrency.rs): `BackendSel`, `WorkloadSel`, `parse_concurrency`, `sweep_order`, `median`, `ArmResult`, `aggregate_tok_s`, `Point`, `render_table`, `ConcurrencyBackend`, `check_k`, `STREAM_PROMPTS`, +10 more
- [`src/main.rs`](src/main.rs): —
- [`src/serve/complete.rs`](src/serve/complete.rs): —
- [`src/serve/http.rs`](src/serve/http.rs): —
- [`src/serve/mod.rs`](src/serve/mod.rs): `complete`, `http`, `slots`
- [`src/serve/slots.rs`](src/serve/slots.rs): —
- [`src/setup.rs`](src/setup.rs): —

### Dependencies (from `Cargo.toml`)

- path: `hipfire-arch-qwen35`, `hipfire-client`, `hipfire-config`, `hipfire-registry`, `hipfire-runtime`
- external: `anyhow`, `clap`, `ctrlc`, `libc`, `serde`, `serde_json`, `sha2`, `tiny_http`, `ureq`
- dev: —
- build: —

### Reverse dependencies

- workspace crates with a path dependency on this crate: —

### Totals

- 7 modules · 17,563 lines · 25 public items · 136 tests · 0 examples

<!-- crate-map:generated:end -->
