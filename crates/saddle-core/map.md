# saddle-core — map

> **Status:** `research` (vocabulary owned by
> [`docs/GLOSSARY.md`](../../docs/GLOSSARY.md), which has a `saddle-core` row).
> **Layer:** substrate — sits below `hipfire-runtime` and every
> `hipfire-arch-*` crate; see the layering table in
> [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md).

## Purpose

Target-agnostic model composition for the saddle substrate: grammar matching,
KV cache, architecture capability contract, sampling policy, and the shared
spec-decode constant. The authoritative design rationale is the `//!` docs in
[`src/lib.rs`](src/lib.rs); the layering contract is the comment block in
[`Cargo.toml`](Cargo.toml).

## Gotchas

- May depend only on `rdna-compute`, `hip-bridge`, `serde`, and `std`. Never
  `hipfire-runtime`, never `hipfire-dispatch`, never any `hipfire-arch-*` —
  a cycle here defeats the layer.
- Abstract the model, never the kernel. If a unification requires naming an
  arch-specific type or kernel, it does not belong here (see the B2 findings
  in [`src/spec.rs`](src/spec.rs)).
- `caps` is declared by the carrier, never inferred from an `arch_id` inside
  this crate.

## Crate map

<!-- crate-map:generated:begin -->

_Generated by `scripts/check-crate-maps.py` from the tree — do not edit inside the markers._

### Modules

| File | Lines | Public items | Tests |
|---|---:|---:|---:|
| [`src/caps.rs`](src/caps.rs) | 208 | 3 | 2 |
| [`src/grammar.rs`](src/grammar.rs) | 3,977 | 6 | 106 |
| [`src/kv.rs`](src/kv.rs) | 4,308 | 13 | 9 |
| [`src/lib.rs`](src/lib.rs) | 72 | 5 | 0 |
| [`src/sampling.rs`](src/sampling.rs) | 52 | 1 | 0 |
| [`src/spec.rs`](src/spec.rs) | 323 | 2 | 1 |

### Public API surface

- [`src/caps.rs`](src/caps.rs): `DflashKind`, `ArchCaps`, `BatchEligibilityRequest`
- [`src/grammar.rs`](src/grammar.rs): `json`, `State`, `ToolSchema`, `Config`, `Matcher`, `dsml`
- [`src/kv.rs`](src/kv.rs): `KvMode`, `KvBackend`, `ParseKvBackendError`, `KV_BACKEND_NAMES`, `DEFAULT_KV_CHUNK_TOKENS`, `DEFAULT_VMM_PHYSICAL_CHUNK_BYTES`, `KvMapGrowth`, `KvChunkPlan`, `KvChunkPlanError`, `VMode`, `KvCache`, `KvLayers`, +1 more
- [`src/lib.rs`](src/lib.rs): `grammar`, `kv`, `caps`, `sampling`, `spec`
- [`src/sampling.rs`](src/sampling.rs): `SamplingDefaults`
- [`src/spec.rs`](src/spec.rs): `MTP_MAX_N`, `clamp_mtp_max_n`

### Dependencies (from `Cargo.toml`)

- path: `hip-bridge`, `rdna-compute`
- external: `serde`
- dev: —
- build: —

### Reverse dependencies

- workspace crates with a path dependency on this crate: `hipfire-arch-deepseek4`, `hipfire-arch-qwen35`, `hipfire-daemon`, `hipfire-engine`, `hipfire-generate`, `hipfire-loader`, `hipfire-runtime`, `saddle-lab`

### Totals

- 6 modules · 8,940 lines · 30 public items · 118 tests · 0 examples

<!-- crate-map:generated:end -->
