[package]
name = "hipfire-lock"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reusable flock(2) file-lock primitive shared by the daemon singleton lock and the GPU mutex."

[dependencies]
libc = "0.2"
