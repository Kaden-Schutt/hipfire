[package]
name = "hipfire-daemon-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Daemon JSONL protocol contracts for hipfire"

[dependencies]
hipfire-generate = { path = "../hipfire-generate" }
hipfire-kld = { path = "../hipfire-kld" }
hipfire-model = { path = "../hipfire-model" }
hipfire-prompt = { path = "../hipfire-prompt" }
serde.workspace = true
serde_json = "1"
