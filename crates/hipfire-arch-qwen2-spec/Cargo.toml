[package]
name = "hipfire-arch-qwen2-spec"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lean offline spec for the Qwen2/Qwen3 dense family (arch_id 1): identity + Ingest quant-policy, no runtime/kernel deps."

[dependencies]
hipfire-arch-api = { path = "../hipfire-arch-api" }
