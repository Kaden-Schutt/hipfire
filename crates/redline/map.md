# redline — map

> **Status:** `research` (vocabulary owned by [`docs/GLOSSARY.md`](../../docs/GLOSSARY.md)).
> **Layer:** Direct-KMD research — bare `libdrm` dispatch that bypasses the HIP runtime, not the serving transport; see the layering table in [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md). GLOSSARY name `Redline` is `research`.

## Purpose

Direct-KMD GPU engine that bypasses `libamdhip64` and talks to `/dev/dri/renderD128` via `libdrm_amdgpu.so` (thin DRM userspace, only the `amdgpu` driver required). Owns `device.rs`/`memory.rs`/`queue.rs`/`dispatch.rs`/`sync.rs` — GPU init, BO alloc/VA mapping, compute queues/PM4 submission, kernel load from `.hsaco`, fences. The `//!` docs in [`src/lib.rs`](src/lib.rs) carry the HIP→Redline ioctl table. Normative contributor procedure is `docs/REDLINE.md` (`branch-implemented` vs `origin/beta`).

## Gotchas

- Not the serving transport — default remains HIP/`hip-bridge`; Redline is gated behind the `redline-dispatch` replay and env-gated trials.
- Requires only the `amdgpu` KMD, not a full ROCm install, but still needs `libdrm_amdgpu.so` available via `dlopen`.
- Parsed `.hsaco` ELF kernel descriptors are the source of truth for launch geometry — `hipModuleLoad`-style caching does not apply.

## Crate map

<!-- crate-map:generated:begin -->

_Generated by `scripts/check-crate-maps.py` from the tree — do not edit inside the markers._

### Modules

| File | Lines | Public items | Tests |
|---|---:|---:|---:|
| [`src/device.rs`](src/device.rs) | 241 | 3 | 0 |
| [`src/dispatch.rs`](src/dispatch.rs) | 563 | 6 | 0 |
| [`src/drm.rs`](src/drm.rs) | 208 | 19 | 0 |
| [`src/hsaco.rs`](src/hsaco.rs) | 245 | 2 | 0 |
| [`src/kfd.rs`](src/kfd.rs) | 512 | 2 | 0 |
| [`src/lib.rs`](src/lib.rs) | 63 | 9 | 0 |
| [`src/pm4.rs`](src/pm4.rs) | 156 | 6 | 0 |
| [`src/queue.rs`](src/queue.rs) | 170 | 3 | 0 |

### Public API surface

- [`src/device.rs`](src/device.rs): `Device`, `GpuInfo`, `GpuBuffer`
- [`src/dispatch.rs`](src/dispatch.rs): `LoadedModule`, `Kernel`, `CommandBuffer`, `DispatchQueue`, `FastDispatch`, `KernargBuilder`
- [`src/drm.rs`](src/drm.rs): `AmdgpuDeviceHandle`, `AmdgpuBoHandle`, `AmdgpuContext`, `AmdgpuVaHandle`, `AMDGPU_GEM_DOMAIN_VRAM`, `AMDGPU_GEM_DOMAIN_GTT`, `AMDGPU_GEM_CREATE_CPU_ACCESS_REQUIRED`, `AMDGPU_VA_OP_MAP`, `AMDGPU_VA_OP_UNMAP`, `AmdgpuGpuVaRange`, `AmdgpuBoAllocRequest`, `AmdgpuGpuInfo`, +7 more
- [`src/hsaco.rs`](src/hsaco.rs): `KernelMeta`, `HsacoModule`
- [`src/kfd.rs`](src/kfd.rs): `AqlPacket`, `AqlQueue`
- [`src/lib.rs`](src/lib.rs): `drm`, `device`, `dispatch`, `hsaco`, `kfd`, `pm4`, `queue`, `RedlineError`, `Result`
- [`src/pm4.rs`](src/pm4.rs): `PKT3_SET_SH_REG`, `PKT3_DISPATCH_DIRECT`, `PKT3_ACQUIRE_MEM`, `PKT3_RELEASE_MEM`, `AqlDispatchPacket`, `Pm4Builder`
- [`src/queue.rs`](src/queue.rs): `crate`, `AMDGPU_HW_IP_COMPUTE`, `ComputeQueue`

### Dependencies (from `Cargo.toml`)

- path: —
- external: `libc`, `libloading`
- dev: —
- build: —

### Reverse dependencies

- workspace crates with a path dependency on this crate: —

### Totals

- 8 modules · 2,158 lines · 50 public items · 0 tests · 16 examples

<!-- crate-map:generated:end -->
